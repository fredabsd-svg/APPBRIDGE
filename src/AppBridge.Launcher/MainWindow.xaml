<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AppBridge.Launcher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppBridge.Launcher"
    Title="AppBridge Launcher">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Spacing="16" Padding="16">
            <TextBlock Text="Applications" FontSize="24" FontWeight="Bold" />

            <CommandBar ClosedDisplayMode="Minimal">
                <AppBarButton Label="Refresh" Icon="Refresh" Command="{x:Bind ViewModel.LoadApplicationsCommand}" />
            </CommandBar>

            <ListView
                ItemsSource="{x:Bind ViewModel.Applications}"
                SelectedItem="{x:Bind ViewModel.SelectedApplication, Mode=TwoWay}"
                Height="300">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:ApplicationDto">
                        <Grid Padding="8" ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Image
                                Grid.Column="0"
                                Width="40"
                                Height="40"
                                Source="{x:Bind IconUrl}"
                                Stretch="UniformToFill" />
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                <TextBlock Text="{x:Bind DisplayName}" FontWeight="Bold" />
                                <TextBlock Text="{x:Bind Description}" FontSize="12" Foreground="Gray" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button
                    Content="Launch"
                    Command="{x:Bind ViewModel.LaunchApplicationCommand}"
                    IsEnabled="{x:Bind ViewModel.SelectedApplication, Mode=OneWay, Converter={StaticResource ObjectToBooleanConverter}}" />
                <TextBlock
                    x:Name="ErrorMessageBlock"
                    Text="{x:Bind ViewModel.ErrorMessage}"
                    Foreground="Red"
                    VerticalAlignment="Center" />
            </StackPanel>

            <ProgressBar
                IsIndeterminate="True"
                Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>
    </Grid>
</Window>
