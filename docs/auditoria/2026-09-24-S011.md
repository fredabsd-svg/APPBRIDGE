# Sessão S011 — 2026-09-24

**Objetivo:** instalar o SDK .NET 10 solicitado por Frederico e habilitar a compilação do Control
Plane.

**O que foi feito:**
- Leitura de `STATUS.md` (RA-01).
- Instalado o SDK .NET 10.0.401 em `/home/fred/.dotnet` usando o instalador oficial da Microsoft, sem
  alterar pacotes globais do sistema.
- Configurados `DOTNET_ROOT` e `PATH` em `/home/fred/.bashrc` e `/home/fred/.profile`; verificado em
  shell de login com `dotnet --version` e `dotnet --list-sdks`.
- Compilado `src/AppBridge.ControlPlane/AppBridge.ControlPlane.csproj`; build passou com zero warnings
  e zero erros. A compilação revelou e corrigiu a seleção da sobrecarga .NET 10 de `WriteAsJsonAsync`.
- Na primeira execução, o SDK criou um certificado de desenvolvimento HTTPS no perfil do usuário; ele
  não foi marcado como confiável.

**Decisões tomadas:**
- Instalação por usuário em `/home/fred/.dotnet`, apropriada ao ambiente atual sem credencial sudo
  disponível. O SDK escolhido continua na versão principal aprovada, .NET 10 (ADR-0005); nenhum ADR
  novo é necessário.

**Arquivos criados/alterados:**
- `/home/fred/.bashrc`, `/home/fred/.profile` (configuração local de ambiente)
- `src/AppBridge.ControlPlane/Program.cs`
- `docs/STATUS.md`, `docs/CHANGELOG.md`
- `docs/auditoria/2026-09-24-S011.md`

**Pendências:**
- Executar a aplicação e confirmar `/health` e os logs com `correlationId`.
- Demonstrar rastreamento ponta a ponta quando T-504 implementar o endpoint de lançamento.
- E-01 e G-01 continuam pendentes e necessários ao aceite ponta a ponta.

**Riscos identificados:**
- R-023 continua aberto; a instalação do SDK e o build não alteram a dependência de infraestrutura.
- Configuração TLS de produção ainda não foi implementada; o perfil local usa loopback HTTP.

**Checagem de consistência desta sessão (RA-02, item 4):** `./scripts/check-docs.sh` executado após
as atualizações documentais; sem achados. Leitura humana: STATUS e CHANGELOG refletem a instalação,
o build e as pendências sem declarar T-201 ou E-01 concluídos.
