# Sessão S010 — 2026-08-10

**Objetivo:** iniciar a implementação de código do MVP-0a — **T-201** (esqueleto do Control Plane,
health check, log estruturado com correlação), **T-202** (EF Core + PostgreSQL + primeira migração
já com `tenant_id` em todas as tabelas, "sim, segue com T-202"), **T-203** (`TenantContext` +
filtro global no `DbContext`, "sim, segue com T-203"), **T-204** (chaves estrangeiras compostas com
`tenant_id`, "sim, segue com T-204"), **T-205** (`AuditWriter` transacional, "sim, segue com
T-205"), **T-206** (teste automatizado de violação de tenant, "sim, segue com T-206"), **T-207**
(coluna `purpose` e teste de metering — "CONTINUE", depois de um "sim, segue com T-204" que já
estava concluído; esclarecido antes de prosseguir), **T-301** (`POST /v1/auth/session`, "sim,
segue com T-301" — primeira tarefa de E-03) e **T-303** (`/auth/refresh` e `/auth/logout`, "sim,
segue com T-303"), **T-304** (`AuthorizationService` com vigência de permissão, "sim, segue com
T-304"), **T-302** (vínculo identidade → conta AD por SID, "sim, segue com T-302"), **T-401**
(seed de catálogo, primeira tarefa de E-04, "sim, segue com E-04"), **T-402** (`GET
/v1/applications` filtrado por autorização, confirmada por pergunta de esclarecimento após
"sim, segue com T-302" ambíguo), **T-403** (`ETag`/`If-None-Match`, "sim, segue com T-403"),
**T-404** (endpoint de ícone, "sim, segue com T-404" — última tarefa de E-04) e **T-501**
(`RdpDescriptorBuilder`, redirecionado de um pedido de "segue com E-01" que não é executável
nesta sandbox — ver nota de processo abaixo) e **T-502** (`IRdpFileSigner`/`RdpSignExeSigner`,
aprovada como "interface + fake testável", "interface + fake testável, seguimos com T-502").

**O que foi feito:**
- Leitura de `STATUS.md` (RA-01). Estado de entrada: PR #40 aberto com a reconciliação das duas
  linhas de trabalho; nenhum código de produção existia ainda.
- **Ambiente de desenvolvimento instalado nesta sessão:** .NET 10 SDK (10.0.302, via instalador
  oficial — o pacote `dotnet-sdk-10.0` do apt não estava disponível no mirror) e PostgreSQL 16
  (já presente no sistema, apenas iniciado).
- **Correção de sequenciamento**, registrada explicitamente e não aplicada em silêncio: `STATUS.md`
  §3 dizia que E-02 (Control Plane) "depende da infraestrutura existir" (E-01). Isso vale para o
  *deploy* real e para testes de integração contra AD DS/RDS verdadeiros — não para o esqueleto do
  código, que só precisa de um PostgreSQL de desenvolvimento. Corrigido em `STATUS.md` e
  `ROADMAP.md`: E-02 corre **em paralelo** com E-01, não depois. Não é mudança de escopo (RP-07 não
  se aplica) — é ordem de execução.
- Criada a solução `AppBridge.slnx` com dois projetos:
  - `src/AppBridge.ControlPlane.Api` — ASP.NET Core, .NET 10
  - `tests/AppBridge.ControlPlane.Api.Tests` — xUnit
- **T-201 implementada:**
  - `CorrelationIdMiddleware` — resolve o `X-Correlation-Id` do cliente (ou gera um novo se ausente
    ou malformado), devolve no cabeçalho de resposta, e grava **duas linhas de log** por requisição
    (início e fim) com o ID no escopo do logger.
  - `GET /v1/health` via `Microsoft.Extensions.Diagnostics.HealthChecks`, com `ResponseWriter`
    próprio que não vaza detalhe interno (RNF-043). Registrado como container extensível: cada
    dependência real (PostgreSQL, AD DS, certificado de assinatura, `ISessionBackend`) adiciona seu
    próprio `IHealthCheck` quando o código que a acessa existir — não um stub sem lastro.
  - Log estruturado em JSON, UTC, com escopos habilitados.
- **Vulnerabilidade corrigida antes do primeiro commit:** o template trouxe `Microsoft.OpenApi`
  2.0.0 transitivamente, com aviso `NU1903` (alta severidade, GHSA-v5pm-xwqc-g5wc). Fixado em
  `Microsoft.OpenApi` 2.11.0 (a versão 3.x quebra o gerador de código do
  `Microsoft.AspNetCore.OpenApi` 10.0.10 — testado e descartado).
- **7 testes automatizados**, cobrindo o que a implementação promete, não só a estrutura:
  - Health check responde 200 e não vaza nome de máquina nem texto de exceção.
  - Correlation ID: gerado quando ausente, ecoado quando válido, **substituído** quando malformado
    (não confiado verbatim).
  - **O log real contém o correlation ID** — capturado por um `ILoggerProvider` de teste, não
    apenas assumido pela presença do `logger.BeginScope`. Duas requisições concorrentes não
    misturam seus IDs (isolamento por `AsyncLocal`).
- Verificação manual: aplicação rodando de verdade, `curl` contra `/v1/health` nos três cenários
  (sem cabeçalho, com UUID válido, com valor malformado), e inspeção do log JSON gerado
  confirmando o `CorrelationId` nas linhas "Request starting" / "Request finished".

**Decisões tomadas (T-201):** nenhum ADR — decisões de implementação, não de arquitetura.

1. **Health check extensível desde já**, sem checks de dependência inventados. A alternativa
   (stubar checks de AD/RDS/certificado que ainda não existem) criaria a mesma falha que o ADR-0015
   existe para prevenir: aparência de cobertura sem cobertura real.
2. **Log de início/fim de requisição escrito explicitamente no middleware**, em vez de confiar no
   log de hospedagem padrão do ASP.NET Core. A primeira tentativa (só o `BeginScope`, sem log
   próprio) **não emitia nenhuma linha para a requisição** — o escopo existia e nunca era usado.
   Descoberto rodando a aplicação de verdade, não pela leitura do código.
3. **`Microsoft.OpenApi` fixado em 2.x, não 3.x** — 3.x quebra o gerador de código do pacote da
   Microsoft que o projeto usa. Registrado no `.csproj` com comentário explicando a escolha, para
   que a próxima atualização de dependência não repita o teste.

---

**O que foi feito (T-202, aprovada com "sim, segue com T-202"):**
- Criados dois novos projetos na solução: `src/AppBridge.ControlPlane.Domain` (POCOs) e
  `src/AppBridge.ControlPlane.Infrastructure` (EF Core + Npgsql), mais
  `tests/AppBridge.ControlPlane.Infrastructure.Tests`.
- **Domain**: 15 entidades espelhando `MODELO-DE-DADOS.md` exatamente, organizadas em
  `Common/Tenancy/Identity/Catalog/Sessions/Trail`. Hierarquia de base:
  `AuditedEntity` (`Id`, `CreatedAt/By`, `UpdatedAt/By`, `RowVersion`, `DeletedAt/By`) e
  `AppendOnlyEntity` (só `Id`, `CreatedAt`, `CreatedBy` — sem colunas de update/delete, deliberado
  para tabela de trilha, ADR-0011 §5); `ITenantScoped` adiciona `TenantId`. `Tenant` e
  `SigningCertificate` são as duas exceções que não implementam `ITenantScoped` (o primeiro É o
  tenant; o segundo é ativo do Control Plane, não dado de tenant — ADR-0009).
  `AccessEvent.EventType` e `PurgeRun.Outcome` modelados como `string` simples, não `enum`, porque
  `MODELO-DE-DADOS.md` não define um conjunto fechado para eles — disciplina RP-05: não inventar uma
  enumeração que o documento aprovado não contém. Cada referência entre entidades ainda sem chave
  estrangeira composta carrega comentário `TODO(T-204)` apontando para ADR-0011 §4.
- **Infrastructure**: `AppBridgeDbContext` com 15 `DbSet<T>` e 15 `IEntityTypeConfiguration<T>`
  (nomes de tabela, CHECK, índices únicos e parciais — `ix_permission_active`/`ix_session_active` —
  conforme o documento). Três convenções aplicadas em `OnModelCreating`, nesta ordem (documentado em
  comentário, porque a ordem importa): `SnakeCaseNamingConvention` (renomeia só coluna, não
  tabela/constraint/índice — esses são nomeados explicitamente em cada configuração),
  `SnakeCaseEnumConverter` (armazena enum como texto `snake_case`, não o nome bruto do membro C#) e
  `ConcurrencyTokenConvention` (mapeia `RowVersion` para a coluna de sistema `xmin` do PostgreSQL,
  `ValueGenerated.OnAddOrUpdate`, `IsConcurrencyToken = true` — sem contador manual). Escolha
  deliberada de não adicionar o pacote `EFCore.NamingConventions`: a convenção é pequena o bastante
  para não justificar uma dependência nova não revisada.
- `DesignTimeDbContextFactory` para `dotnet ef`, lendo `APPBRIDGE_DB_CONNECTION` do ambiente (nunca
  de arquivo — RP-06).
- Migração `InitialCreate` gerada e **revisada por leitura exaustiva do SQL** (`dotnet ef migrations
  script`): 15 tabelas, `tenant_id NOT NULL` em todas exceto `tenant`/`signing_certificate`, `xmin`
  corretamente ausente do DDL (é coluna de sistema, não criada) mas presente no modelo do EF.
- **PostgreSQL local**: role `appbridge`, bancos `appbridge_dev` (dev) e `appbridge_test` (testes —
  sem Docker/Testcontainers neste ambiente, os testes rodam contra banco real).
- **Verificação manual, não só leitura do SQL gerado**: migração aplicada de fato a `appbridge_dev`;
  inserção de tenant válido; tentativa de violar as duas CHECK e a unicidade de `slug`, confirmando
  rejeição pelo nome exato da constraint; migração revertida (as 15 tabelas somem, histórico de
  migração esvaziado) e reaplicada com sucesso — prova de ida e volta do critério de aceite
  ("Migração aplica e reverte").
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/SchemaTests.cs`: 8 testes automatizados contra
  `appbridge_test`, usando `IMigrator` (via `context.GetInfrastructure().GetRequiredService<IMigrator>()`)
  para migrar para baixo e para cima dentro do próprio teste — existência das 15 tabelas,
  não-nulidade de `tenant_id` nas 13 tabelas com escopo de tenant, as duas exceções deliberadas, as
  duas CHECK (via `PostgresException.ConstraintName` real, não assumido), unicidade de `slug`, e
  prova de que o `Id` é gerado pela aplicação antes do `SaveChanges` (UUID v7), não pelo banco.
- **Erro encontrado e corrigido durante o build do projeto de teste**: `dotnet build` terminou com
  sucesso mas emitiu `MSB3277` — conflito entre `Microsoft.EntityFrameworkCore.Relational` 10.0.4 e
  10.0.10. Diagnosticado com `dotnet list package --include-transitive`: o pacote
  `Npgsql.EntityFrameworkCore.PostgreSQL` 10.0.3 (a versão mais recente publicada até esta data) traz
  a 10.0.4 transitivamente, em conflito com a 10.0.10 referenciada diretamente para o restante do EF
  Core. Corrigido fixando `Microsoft.EntityFrameworkCore.Relational` explicitamente em 10.0.10 no
  `.csproj` da Infrastructure, com comentário explicando o motivo. Rebuild limpo, 0 *warnings*.
  `dotnet test` executado em seguida contra `appbridge_test`: **8 de 8 passando**.
- Dois pequenos erros de compilação corrigidos no caminho (`SnakeCaseEnumConverter.Parse` sem ser
  genérico; `ConcurrencyTokenConvention` sem `using Microsoft.EntityFrameworkCore.Metadata`) — sem
  relevância documental além do próprio código.
- **Autocorreção antes de commitar**: o primeiro comentário sobre a CHECK de `redirection_policy`
  dizia que ela "garante" que o valor de override diverge da política base do tenant — mas uma CHECK
  de uma única linha não consegue comparar contra outra linha (a linha base). Corrigido o comentário
  para afirmar exatamente o que a CHECK garante (a presença de `exception_reason`, um proxy
  verificável na mesma linha), não o que ela não garante.

**Decisões tomadas (T-202):** nenhum ADR — decisões de implementação dentro do que ADR-0011 já
havia decidido.

1. **`xmin` do PostgreSQL como token de concorrência**, em vez de um contador `row_version`
   incrementado manualmente — menos lugar para a lógica de incremento estar errada, e é a
   recomendação do próprio provedor Npgsql para EF Core.
2. **Convenção de `snake_case` e de enum escritas à mão**, não via pacote de terceiro — evita uma
   dependência nova não revisada para um problema pequeno.
3. **`EventType`/`Outcome` como `string`, não `enum`**, porque o documento de modelo de dados
   aprovado não fecha o conjunto de valores — inventar um enum seria RP-05 violado silenciosamente.
4. **Chave estrangeira composta com `tenant_id` (ADR-0011 §4) deferida para T-204**, como o
   `ROADMAP.md` já previa — não é corte de escopo, é a ordem que já estava planejada.

**Arquivos criados/alterados:**
- `AppBridge.slnx` (alterado — três projetos novos)
- `src/AppBridge.ControlPlane.Api/` — `Program.cs`, `Middleware/CorrelationIdMiddleware.cs`,
  `HealthChecks/HealthCheckResponseWriter.cs`, `.csproj`, `appsettings*.json`, `Properties/` (criados)
- `tests/AppBridge.ControlPlane.Api.Tests/` — `HealthCheckTests.cs`,
  `CorrelationIdMiddlewareTests.cs`, `RequestLoggingTests.cs`, `.csproj` (criados)
- `src/AppBridge.ControlPlane.Domain/` — 15 entidades e classes base (criado)
- `src/AppBridge.ControlPlane.Infrastructure/` — `AppBridgeDbContext.cs`, `Conventions/*.cs`,
  `Configurations/*.cs` (15), `DesignTimeDbContextFactory.cs`, `Migrations/*InitialCreate*` (criado)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/` — `SchemaTests.cs`, `.csproj` (criado)
- `docs/SETUP-DEV.md` (criado — ambiente de desenvolvimento local)
- `docs/ROADMAP.md` (alterado — T-201 e T-202 marcadas concluídas, notas de conclusão)
- `docs/STATUS.md` (alterado — fase, §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências:**
- **T-203** é a próxima: `TenantContext` + filtro global no `DbContext` (ADR-0004). O filtro de
  tenant **ainda não está ligado** em `AppBridgeDbContext` — comentário explícito no código diz que
  isso é responsabilidade de T-203, para que ninguém leia o `DbContext` de hoje como já-isolado por
  tenant.
- **T-204**: chave estrangeira composta com `tenant_id` — cada referência entre entidades no Domain
  já carrega o `TODO(T-204)` correspondente.
- O health check de T-201 ainda não verifica PostgreSQL (nem nenhuma outra dependência real) — seguirá
  assim até a tarefa que efetivamente usa o `AppBridgeDbContext` em runtime registrar seu próprio
  `IHealthCheck`, não antes.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-201/T-202):**
- **Risco de verificação superficial, evitado duas vezes nesta sessão.** Em T-201, a primeira versão
  do middleware de correlação parecia correta por inspeção mas não emitia log nenhum — só apareceu
  rodando a aplicação de verdade. Em T-202, o mesmo princípio se aplicou à migração: gerar e ler o SQL
  não bastou como prova de "aplica e reverte" — foi preciso rodá-la de fato contra PostgreSQL real,
  inserir dado inválido e confirmar a rejeição pelo nome da constraint.
- **Dependência de terceiro atrás do cronograma do EF Core.** `Npgsql.EntityFrameworkCore.PostgreSQL`
  ainda não publicou uma versão alinhada com `Microsoft.EntityFrameworkCore` 10.0.10; o pino manual
  em `Microsoft.EntityFrameworkCore.Relational` é uma correção válida mas frágil a uma próxima
  atualização de pacote — revisitar quando o Npgsql lançar uma versão nova.
- **Nenhum risco novo de produto.** É trabalho de fundação, dentro do que ADR-0011 já decidiu.

---

**O que foi feito (T-203, aprovada com "sim, segue com T-203"):**
- `src/AppBridge.ControlPlane.Infrastructure/Tenancy/ITenantContext.cs` — `Guid? TenantId { get; }`,
  nulo até algo o resolver. `TenantContext` — implementação simples, mutável, para injeção *scoped*
  por requisição (a resolução real a partir do token é do middleware de T-301; esta tarefa só
  precisava de onde ele vai escrever).
- `AppBridgeDbContext` passou a receber `ITenantContext` no construtor e aplica, em
  `OnModelCreating`, um filtro global por tipo de entidade (via reflexão, percorrendo
  `modelBuilder.Model.GetEntityTypes()`): quem implementa `ITenantScoped` **e** deriva de
  `AuditedEntity` recebe `TenantId == contexto.TenantId && DeletedAt == null`; quem só tem uma das
  duas características recebe só a cláusula correspondente; `Launch`/`AccessEvent`/`PurgeRun`
  (`AppendOnlyEntity`, sem `DeletedAt`) recebem só a de tenant.
- **A metade de `DeletedAt` não é decisão desta tarefa** — é a execução do que ADR-0011 §5 já havia
  decidido ("toda consulta considere `deleted_at`... junto com o filtro de tenant"), agora que o
  `DbContext` tem de onde ler o tenant corrente para os dois ao mesmo tempo. Registrado em comentário
  no próprio código, para que quem ler não confunda com uma decisão nova de T-203.
- `DesignTimeDbContextFactory` e `SchemaTests` atualizados para o novo construtor (com
  `TenantContext` vazio — migrações e os testes de esquema operam em DDL/SQL cru, não em `DbSet`
  filtrado, então um contexto sem tenant não muda o que já era verificado).
- **`tests/AppBridge.ControlPlane.Infrastructure.Tests/TenantIsolationTests.cs`** — 4 testes, todos
  contra PostgreSQL real (mesma abordagem de T-202):
  1. Consulta sem `Where` explícito, em contexto do tenant A, devolve só a linha do tenant A — não a
     do tenant B, inserida por um contexto separado.
  2. Consulta em contexto **sem** tenant resolvido devolve **zero linhas**, não todas — prova de que
     o isolamento falha fechado, não aberto.
  3. Linha com exclusão lógica (`DeletedAt` preenchido) some da consulta padrão e só reaparece com
     `IgnoreQueryFilters()`.
  4. `IgnoreQueryFilters()` também devolve dado de outro tenant quando chamado explicitamente — prova
     de que o filtro é um padrão contornável de propósito (ADR-0004 item 7), não uma parede sem
     saída, que é exatamente o mecanismo que o papel de operador do provedor (RF-075, MVP-1) vai usar
     de forma nominal e auditada.
- **Erro de infraestrutura de teste encontrado e corrigido**: rodar `SchemaTests` junto com
  `TenantIsolationTests` falhou com `relation "application" does not exist` em 2 dos 12 testes.
  Diagnosticado como corrida, não bug do filtro: as duas classes migram o mesmo banco
  `appbridge_test` para baixo e para cima dentro de `InitializeAsync`, e o xUnit paraleliza classes
  de teste por padrão — uma classe apagava tabelas que a outra estava consultando. Corrigido com
  `[assembly: CollectionBehavior(DisableTestParallelization = true)]`: o banco real e compartilhado
  é um recurso serial enquanto não houver Testcontainers, então a suíte roda serial também.
- **19 testes automatizados no total** no Control Plane (7 Api + 8 Schema + 4 TenantIsolation),
  todos passando contra PostgreSQL real nesta sessão.

**Decisões tomadas (T-203):** nenhum ADR — execução de ADR-0004 e de uma consequência já registrada
em ADR-0011 §5.

1. **Filtro combinado (tenant + soft delete) no mesmo `HasQueryFilter`**, por tipo de entidade, em
   vez de dois filtros separados — é o que ADR-0011 §5 já previa, e o EF Core só permite um
   `HasQueryFilter` por tipo de qualquer forma.
2. **Reflexão para aplicar o filtro certo a cada entidade**, em vez de repetir a expressão 15 vezes
   nas classes de configuração — evita que uma entidade nova esqueça o filtro por engano; o
   `OnModelCreating` decide pela interface/classe base, não pela memória de quem escreveu a
   configuração.
3. **`ITenantContext.TenantId` nulo como "sem tenant", com filtro que devolve zero linhas** — a
   alternativa (lançar exceção quando não resolvido) quebraria qualquer código que ainda não passou
   pelo middleware de T-301; a alternativa oposta (tratar nulo como "sem filtro") reabriria
   exatamente o vazamento que ADR-0004 existe para fechar. Falhar fechado é o meio-termo seguro.
4. **`IgnoreQueryFilters()` nativo do EF Core como mecanismo de travessia**, em vez de um método
   próprio — é explícito no código (ADR-0004 item 7) e não precisa de abstração nova; a auditoria da
   travessia (RNF-017, `acting_as_provider`) fica para quando RF-075 for implementado em MVP-1.

**Arquivos criados/alterados (T-203):**
- `src/AppBridge.ControlPlane.Infrastructure/Tenancy/ITenantContext.cs`,
  `Tenancy/TenantContext.cs` (criados)
- `src/AppBridge.ControlPlane.Infrastructure/AppBridgeDbContext.cs` (alterado — construtor, filtro
  global)
- `src/AppBridge.ControlPlane.Infrastructure/DesignTimeDbContextFactory.cs` (alterado)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/SchemaTests.cs` (alterado — novo construtor)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/TenantIsolationTests.cs`,
  `AssemblyInfo.cs` (criados)
- `docs/ROADMAP.md` (alterado — T-203 marcada concluída, nota de conclusão)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-203, na ordem em que a sessão de fato seguiu):**
- ~~**T-204** é a próxima: chave estrangeira composta com `tenant_id` (ADR-0011 §4)~~ — **concluída
  na sequência desta mesma sessão**, ver abaixo.
- Nenhuma tabela ainda tem `TenantId` preenchido automaticamente na escrita — quem insere hoje
  precisa passar o valor explicitamente (como os testes fazem). Não é lacuna desta tarefa: T-203
  cobre a leitura (ADR-0004 item 6), e nenhuma decisão registrada exige carimbo automático na
  escrita. Se isso vier a ser necessário, é decisão nova, não extensão silenciosa de T-203.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-203):**
- **Corrida de teste já fechada, mas o padrão se repete a cada suíte nova que migra o mesmo banco.**
  Qualquer novo conjunto de testes de Infraestrutura que também rode `IMigrator` contra
  `appbridge_test` herda o mesmo risco até existir isolamento por schema/Testcontainers — hoje
  contido por rodar a suíte inteira serial, o que já é uma escolha assumida (mais lenta, mais
  simples).
- **Filtro correto hoje depende de todo `DbSet` passar sempre pelo `AppBridgeDbContext`** — uma
  consulta que abrisse uma conexão Npgsql crua (como os testes de CHECK constraint fazem
  deliberadamente) não é protegida por ele. Aceitável para os próprios testes, que testam a
  constraint, não o isolamento; mas é um lembrete de que o filtro protege consultas via EF, não
  todo acesso ao banco — reforça PD-02 (Row-Level Security como terceira linha de defesa, `STATUS.md`
  §9).
- **Nenhum risco novo de produto.** ADR-0004 já previa exatamente este mecanismo.

---

**O que foi feito (T-204, aprovada com "sim, segue com T-204"):**
- Levantamento de toda referência entre entidades de tenant a partir dos comentários `TODO(T-204)`
  já deixados no Domain (10 ocorrências, 6 relacionamentos) **mais** uma releitura de
  `MODELO-DE-DADOS.md` para achar o que faltava marcar: `redirection_policy.application_id`,
  `application_permission.granted_by`/`revoked_by`, `launch.session_id` e
  `access_event.user_account_id` estavam documentados como FK no modelo aprovado mas sem o
  comentário no código — corrigidos junto, não deixados como uma segunda pendência.
- `EntityConfigurationExtensions.cs` ganhou dois métodos de apoio, usados por praticamente toda
  configuração: `ConfigureTenantForeignKey<TEntity>()` (FK simples `tenant_id -> tenant(id)`,
  `RESTRICT`, nome derivado do tipo via o `ToSnakeCase` que `SnakeCaseNamingConvention` já expunha)
  e `ConfigureTenantAlternateKey<TEntity>()` (`UNIQUE (tenant_id, id)` — a "chave candidata" que o
  próprio ADR-0011 §4 nomeia). As 15 chaves estrangeiras compostas em si foram escritas explicitamente
  em cada `IEntityTypeConfiguration`, sem uma abstração genérica adicional: cada uma difere em tipo
  principal, nulabilidade ou nome de constraint o bastante para que uma árvore de expressão genérica
  custasse mais em complexidade do que economizasse em repetição — e o estilo explícito espelha a SQL
  literal do exemplo do próprio ADR-0011 (`fk_permission_application` é o mesmo nome do exemplo).
- Resultado: **15 FKs compostas**, **6 chaves alternativas** (`application`, `group`, `host_pool`,
  `session_host`, `session`, `user_account` — as seis entidades que outra tabela referencia) e **13
  FKs simples** `tenant_id -> tenant(id)` (uma por tabela com escopo de tenant, já declarada como
  `uuid FK` em `MODELO-DE-DADOS.md` mas nunca antes ligada ao banco). Todas com `ON DELETE RESTRICT`:
  dado de tenant nunca é fisicamente removido (ADR-0011 §3), então a restrição só dispararia diante
  de um `DELETE` indevido.
- Migração `AddTenantForeignKeys` gerada, **revisada por leitura completa do arquivo gerado** (não só
  do diff): 6 `AddUniqueConstraint`, 13+15 `AddForeignKey`, e os índices de suporte que o EF Core
  cria automaticamente nas colunas de FK que ainda não tinham um. `Down()` reverte tudo na ordem
  inversa correta.
- **Verificado com PostgreSQL real, aplicando a favor e contra**: migração aplicada a `appbridge_dev`;
  via `psql`, uma tentativa de gravar `application` do tenant B apontando para `host_pool` do tenant
  A foi **recusada** com o nome de constraint exato (`fk_application_host_pool`, mensagem "Key
  (tenant_id, host_pool_id)=(...) is not present in table host_pool"); a mesma gravação, com o par
  `tenant_id`/`host_pool_id` correspondente ao mesmo tenant, foi **aceita**. Essa é exatamente a
  redação do critério de aceite de T-204.
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/TenantForeignKeyTests.cs` — 2 testes fixando essa
  mesma prova como regressão automatizada (mesmo padrão de `PostgresException.ConstraintName` usado
  nos testes de CHECK constraint de T-202): a escrita cruzada falha com o nome certo; a escrita
  correta é aceita.
- **Efeito colateral encontrado e corrigido**: `TenantIsolationTests.cs` (T-203) criava cada
  `Application` com `HostPoolId = Guid.CreateVersion7()` — um id fabricado, inofensivo antes de T-204
  porque nada verificava a referência. Rodar a suíte depois desta tarefa quebrou 4 dos seus próprios
  testes com `fk_application_host_pool` — não um bug do filtro de tenant, mas um teste que dependia
  implicitamente da ausência da proteção que esta própria tarefa adiciona. Corrigido criando um
  `HostPool` real por tenant em `InitializeAsync` e referenciando o id certo.
- **21 testes automatizados no total** no Control Plane (7 Api + 8 Schema + 4 TenantIsolation + 2
  TenantForeignKey), todos passando contra PostgreSQL real nesta sessão.

**Decisões tomadas (T-204):** nenhum ADR — execução de ADR-0011 §4, incluindo três relacionamentos
que o modelo de dados já previa e o código ainda não marcava.

1. **`ON DELETE RESTRICT` em toda FK nova**, em vez do `CASCADE` que o EF Core usaria por padrão em
   relacionamentos obrigatórios — ADR-0011 §3 já decidiu que dado de tenant só se apaga logicamente;
   `RESTRICT` faz o banco recusar qualquer `DELETE` físico que a aplicação nunca deveria emitir, em
   vez de silenciosamente arrastar linhas de trilha ou zerar uma referência histórica.
2. **Métodos de apoio só para os dois padrões 100% uniformes** (FK simples para `tenant`, chave
   alternativa `(tenant_id, id)`); as 15 FKs compostas entre entidades ficaram explícitas — a
   diferença real entre elas (tipo principal, nulidade, nome) tornaria uma abstração genérica mais
   difícil de auditar do que 15 blocos que se leem quase como a SQL do ADR.
3. **Corrigir os relacionamentos sem `TODO` junto com os que tinham**, em vez de abrir uma pendência
   nova — estavam documentados em `MODELO-DE-DADOS.md` desde a aprovação do entregável 4; deixá-los
   de fora seria a mesma classe de deriva que a S006 (auditoria de consistência) já havia caçado uma
   vez.

**Arquivos criados/alterados (T-204):**
- `src/AppBridge.ControlPlane.Infrastructure/Conventions/EntityConfigurationExtensions.cs` (alterado
  — `ConfigureTenantForeignKey`, `ConfigureTenantAlternateKey`)
- `src/AppBridge.ControlPlane.Infrastructure/Configurations/*.cs` (12 dos 15 arquivos alterados —
  todos exceto `TenantConfiguration.cs` e `SigningCertificateConfiguration.cs`, que não têm coluna
  `tenant_id`)
- `src/AppBridge.ControlPlane.Infrastructure/Migrations/20260810123525_AddTenantForeignKeys.cs`
  e `.Designer.cs`, `AppBridgeDbContextModelSnapshot.cs` (criados/alterados)
- `src/AppBridge.ControlPlane.Domain/**/*.cs` (6 arquivos — comentários `TODO(T-204)` substituídos
  pelo nome real da constraint; 3 arquivos a mais ganharam o comentário pela primeira vez)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/TenantForeignKeyTests.cs` (criado)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/TenantIsolationTests.cs` (alterado — `HostPool`
  real por tenant)
- `docs/ROADMAP.md` (alterado — T-204 marcada concluída, nota de conclusão)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-204, na ordem em que a sessão de fato seguiu):**
- ~~**T-205** (`AuditWriter` transacional) é a próxima~~ — **concluída na sequência desta mesma
  sessão**, ver abaixo.
- **T-206** (teste automatizado de violação de tenant, V-02) agora tem as duas metades possíveis:
  leitura (T-203) e escrita (T-204). T-204 provou o mecanismo com um relacionamento
  (`application`/`host_pool`); T-206 é quem cobre sistematicamente os 15, idealmente através da
  camada de serviço que ainda não existe, não só via SQL cru como os testes desta sessão.
- Nenhuma tabela ainda carimba `TenantId` automaticamente na escrita — continua não sendo lacuna de
  T-204 (que é sobre o banco recusar um valor errado, não sobre preencher um valor certo); se um
  `AuditWriter` ou serviço futuro precisar disso, é decisão desse momento.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-204):**
- **Índices de suporte às novas FKs foram gerados automaticamente pelo EF Core**, não desenhados à
  mão — cobrem exatamente as colunas de cada FK, o que é o padrão correto para não deixar toda
  gravação em tabela referenciada varrendo a tabela referenciadora sem índice, mas não foram
  avaliados quanto a redundância com os índices já existentes (`ix_permission_active`,
  `ix_session_active` etc.). Revisar sob carga real (T-005) antes do piloto, não antes.
- **`ON DELETE RESTRICT` só é testado no caminho feliz e no caminho de violação de tenant nesta
  sessão** — não foi exercitado um `DELETE` físico genuíno tentando apagar, por exemplo, um
  `host_pool` com aplicativos publicados. Comportamento esperado por leitura da migração, não
  verificado por execução; nenhuma tarefa do roadmap prevê emitir `DELETE` físico, então o risco é
  baixo, mas fica registrado.
- **Nenhum risco novo de produto.** ADR-0011 §4 já previa exatamente este mecanismo, com o exemplo
  `application`/`application_permission` que os testes desta sessão reproduzem.

---

**O que foi feito (T-205, aprovada com "sim, segue com T-205"):**
- `src/AppBridge.ControlPlane.Infrastructure/Auditing/IAuditWriter.cs` — a interface que toda
  operação de segurança (RF-036 autenticação, RF-037 lançamento, RF-039 negativa de autorização,
  RF-041 ação administrativa, RF-042 uso de certificado) vai chamar. Dois métodos:
  `ExecuteAsync<TAudit, TResult>` (com retorno) e `ExecuteAsync<TAudit>` (sem retorno, delega para o
  primeiro). Deliberadamente **não** cobre telemetria, métricas, indicador de latência, log de
  diagnóstico do launcher nem fim de sessão (RF-038) — são as exceções que o próprio ADR-0007 declara.
- `Auditing/AuditWriter.cs` — implementação: adiciona a entrada de auditoria ao `DbContext`, executa
  `grant` (a mutação de estado da concessão) contra o mesmo `DbContext`, e chama `SaveChangesAsync`
  uma única vez. `grant` é **síncrono e só mexe no banco** por desenho — a própria assinatura do
  método (`Func<AppBridgeDbContext, TResult>`, sem `async`) impede que um efeito colateral com E/S
  real (assinar um `.rdp`, chamar `ISessionBackend`) entre no limite transacional; esse efeito tem
  que vir depois de um commit bem-sucedido, nunca dentro dele — a mesma disciplina de "mecanismo, não
  lembrete" já usada no filtro de tenant (T-203) e nas FKs compostas (T-204), aplicada agora à
  atomicidade auditoria+concessão.
- Em caso de falha em qualquer ponto (o `grant`, o `SaveChangesAsync`, o próprio banco), `AuditWriter`
  registra um log de erro estruturado — porque a própria trilha em banco é o que falhou, e a condição
  2 do ADR-0007 exige que a falha sobreviva em algum lugar — e relança como
  `Auditing/AuditWriteFailedException.cs`, um tipo próprio, não a `DbUpdateException` crua. O motivo
  de existir como tipo distinto: um endpoint futuro (T-301 em diante) precisa distinguir "a trilha
  falhou" (que deve virar `503 AUDIT_UNAVAILABLE`, API.md/ADR-0012) de qualquer outra falha de banco
  (que é um erro diferente) — sem esse tipo, o catch do endpoint teria que inspecionar a exceção do
  Npgsql para adivinhar qual dos dois aconteceu.
- **Verificado com uma falha de gravação simulada, não hipotética**: `ThrowingSaveChangesInterceptor`
  (um `SaveChangesInterceptor` de teste) lança exatamente onde o EF Core emitiria o SQL de escrita —
  a janela específica que o ADR-0007 fecha (o banco lê mas não escreve; se ele nem lê, a autorização
  já falha antes de chegar aqui, então esse outro caso não precisa de teste próprio).
  `tests/AppBridge.ControlPlane.Infrastructure.Tests/AuditWriterTests.cs` — 3 testes:
  1. Caminho feliz: a entrada de auditoria e a mutação da concessão (`UserAccount.LastLoginAt`)
     aparecem juntas quando lidas de volta por um contexto limpo.
  2. Com a falha simulada: `ExecuteAsync` lança `AuditWriteFailedException` e, lido de volta,
     **nem a linha de auditoria nem `LastLoginAt` foram gravados** — a negação é da operação inteira,
     comprovando o critério de aceite literal de T-205.
  3. A falha é logada (nível `Error`, com a exceção original anexada) antes do relançamento.
- **Erro de teste corrigido, não de produto**: o primeiro teste do caminho feliz comparava
  `DateTimeOffset` por igualdade exata e falhou por um dígito — `timestamptz` do PostgreSQL arredonda
  para microssegundos, e o tick do .NET tem resolução mais fina. Corrigido comparando com tolerância
  de 1 ms (`Assert.Equal(expected, actual, TimeSpan.FromMilliseconds(1))`).
- **Interrupção de ambiente, sem relação com o código desta tarefa**: o PostgreSQL local havia caído
  entre a sessão anterior e esta parte da sessão (`service postgresql status` → `down`, provavelmente
  por reinício do contêiner). Reiniciado (`service postgresql start`) antes de rodar os testes.
  Registrado em `docs/SETUP-DEV.md` §6 como armadilha conhecida, para a próxima vez que isso acontecer
  não custar tempo de diagnóstico.
- **24 testes automatizados no total** no Control Plane (7 Api + 8 Schema + 4 TenantIsolation + 2
  TenantForeignKey + 3 AuditWriter), todos passando contra PostgreSQL real nesta sessão.

**Decisões tomadas (T-205):** nenhum ADR — execução de ADR-0007 Parte 1.

1. **`grant` como `Func` síncrono, não `Func<Task>`** — decisão de desenho deliberada para que a
   assinatura do método torne estruturalmente impossível colocar uma chamada de E/S externa dentro do
   limite transacional, em vez de depender de um comentário ou de revisão de código para pegar isso.
2. **`AuditWriteFailedException` como tipo próprio**, em vez de deixar a `DbUpdateException` do EF
   Core vazar — o consumidor futuro (T-301) precisa reagir diferente a "a trilha falhou" do que a
   qualquer outro erro de banco, e um tipo dedicado é o único jeito de fazer essa distinção sem
   inspecionar mensagem de exceção.
3. **Falha de gravação simulada via `SaveChangesInterceptor`, não via desligar o banco de verdade** —
   mais preciso (ataca exatamente o ponto que o ADR-0007 discute — o banco que lê mas não escreve) e
   repetível, sem depender de manipular permissões ou espaço em disco do PostgreSQL de teste a cada
   execução.
4. **`IAuditWriter` genérico sobre `TAudit`**, não fixado em `AccessEvent` — já funciona para `Launch`
   hoje e para `AdminAuditEvent`/trilha de certificado quando essas tabelas existirem (MVP-1/V2), sem
   precisar alterar `AuditWriter` outra vez.

**Arquivos criados/alterados (T-205):**
- `src/AppBridge.ControlPlane.Infrastructure/Auditing/IAuditWriter.cs`, `AuditWriter.cs`,
  `AuditWriteFailedException.cs` (criados)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/AuditWriterTests.cs` (criado)
- `docs/ROADMAP.md` (alterado — T-205 marcada concluída, nota de conclusão)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/SETUP-DEV.md` (alterado — §6, PostgreSQL caído entre sessões)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-205, na ordem em que a sessão de fato seguiu):**
- ~~**T-206** (teste automatizado de violação de tenant, V-02) é a próxima~~ — **concluída na
  sequência desta mesma sessão**, ver abaixo.
- `AuditWriter` **ainda não está registrado no contêiner de DI** da Api (`Program.cs`) — decisão
  deliberada, não esquecimento: não há nenhum endpoint que o consuma ainda (T-301 é o primeiro). Ligar
  `AppBridgeDbContext`/`ITenantContext`/`IAuditWriter` ao `Program.cs` fica para a tarefa que
  primeiro precisar deles em tempo de execução, para não registrar dependência sem consumidor real
  (mesmo raciocínio já aplicado ao health check de PostgreSQL em T-201/T-202).
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-205):**
- **`grant` síncrono é uma restrição de desenho que só se prova certa quando a primeira operação real
  (T-301) tentar violá-la.** Se o login precisar mesmo de uma chamada de E/S dentro do mesmo limite
  transacional (não deveria, mas ainda não foi construído), a decisão 1 acima precisa ser revisitada
  — não como mudança silenciosa, como nova decisão registrada.
- **PostgreSQL local não sobrevive a reinício do ambiente.** Não é risco de produto (é característica
  do sandbox de desenvolvimento), mas é a segunda vez nesta sessão que o ambiente precisou de
  diagnóstico manual antes de rodar teste — vale considerar automatizar a checagem/start no início de
  sessão futura, se o padrão se repetir.
- **Nenhum risco novo de produto.** ADR-0007 Parte 1 já previa exatamente este mecanismo.

---

**O que foi feito (T-206, aprovada com "sim, segue com T-206"):**
- Levantamento do que T-203 (`TenantIsolationTests.cs`) e T-204 (`TenantForeignKeyTests.cs`) já
  provavam antes de escrever qualquer teste novo, para não duplicar cobertura: os dois mecanismos
  (filtro de leitura, FK composta de escrita) já estavam demonstrados, mas cada um só com **uma**
  forma de entidade/relacionamento (`Application` para leitura; `application`/`host_pool` para
  escrita). T-206 não repetiu essas provas — foi atrás de formas ainda não exercitadas.
- Duas lacunas reais identificadas por leitura do próprio código de `AppBridgeDbContext` e das
  configurações de FK, não por suposição:
  1. `ApplyTenantAndSoftDeleteFilters` tem três ramos (`SetTenantAndSoftDeleteFilter`,
     `SetTenantFilter`, `SetSoftDeleteFilter`); todo teste de isolamento até aqui passava só pelo
     primeiro, porque `Application` é `TenantScopedEntity` (tem `deleted_at`). Nenhuma tabela de
     trilha (`launch`, `access_event`, `purge_run` — só `tenant_id`, sem `deleted_at`) havia sido
     lida por um teste de isolamento.
  2. Toda FK composta testada até aqui era obrigatória e apontava para um tipo principal usado uma
     única vez naquela tabela. `application_permission` tem duas FKs independentes para o mesmo
     tipo (`granted_by`/`revoked_by` → `user_account`) — o padrão mais propenso a esconder um erro
     de cópia-e-cola na configuração (por exemplo, as duas FKs acidentalmente apontando para o
     mesmo par de colunas-sombra). E nenhuma FK **anulável** havia sido testada
     (`redirection_policy.application_id`, `launch.session_id`).
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/TenantViolationTests.cs` — criado como o local
  explícito e nomeado de **V-02** (`SEGURANCA.md` §7), com referência cruzada no próprio comentário
  de classe para AM-07/AM-14/ADR-0004 item 9, para que uma auditoria futura encontre o teste pelo
  nome da verificação, não por acaso. 4 testes:
  1. Leitura sem `Where` explícito em `user_account` — mesma prova de T-203, para um segundo tipo de
     entidade (`TenantScopedEntity` com chave alternativa, referenciada por outras tabelas).
  2. Leitura sem `Where` explícito em `access_event` — primeira prova do ramo `SetTenantFilter`
     sozinho.
  3. Escrita cruzada em `redirection_policy.application_id` (anulável) recusada com
     `fk_redirection_policy_application`.
  4. Escrita cruzada em `application_permission.granted_by` recusada com
     `fk_permission_granted_by` — confirma que a FK certa dispara mesmo havendo uma segunda FK
     (`revoked_by`) para o mesmo tipo principal na mesma tabela.
- **Cobertura deliberadamente não exaustiva, documentada como tal no próprio arquivo** (não como
  lacuna silenciosa): as 13 tabelas com escopo de tenant e as 15 FKs compostas não são testadas uma
  a uma. ADR-0004 item 9 pede "casos que tentam ler e escrever dado de outro tenant e exigem
  falha" — não uma matriz combinatória — e, somando este arquivo a T-203/T-204, os dois mecanismos já
  foram exercitados em cinco formas distintas de entidade/relacionamento (obrigatória, anulável,
  alternate-key, append-only, duas-FKs-mesmo-tipo).
- **28 testes automatizados no total** no Control Plane (7 Api + 8 Schema + 4 TenantIsolation + 2
  TenantForeignKey + 3 AuditWriter + 4 TenantViolation), todos passando contra PostgreSQL real.
- PostgreSQL local havia caído novamente entre a parte anterior da sessão e esta (segunda vez nesta
  sessão) — reiniciado antes de rodar os testes; já coberto pela nota adicionada a
  `docs/SETUP-DEV.md` §6 em T-205, nenhuma alteração nova de documentação necessária por isso.

**Decisões tomadas (T-206):** nenhum ADR — execução de ADR-0004 item 9, com o escopo de cobertura
declarado explicitamente (não exaustivo) em vez de deixado implícito.

1. **Arquivo de teste próprio e nomeado por V-02**, em vez de adicionar mais casos aos arquivos de
   T-203/T-204 — um controle de segurança citado em `SEGURANCA.md` por identificador (V-02) merece
   um lugar único e discoverable na suíte, não ficar disperso como efeito colateral de outras tarefas.
2. **Escolha das lacunas por leitura do código-fonte** (os três ramos de
   `ApplyTenantAndSoftDeleteFilters`; as FKs anuláveis e as de tipo principal duplicado), não por
   tentar cobrir "mais alguma coisa" arbitrária — garante que a cobertura nova testa uma diferença
   real de comportamento, não uma repetição do que já estava provado.
3. **Não perseguir as 13/15 combinações restantes**, com a decisão e o motivo registrados no
   comentário da própria classe de teste — para que a próxima pessoa (ou sessão) que olhar o arquivo
   saiba que a lacuna é deliberada, e não precise redescobrir isso perguntando ou assumindo o pior.

**Arquivos criados/alterados (T-206):**
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/TenantViolationTests.cs` (criado)
- `docs/ROADMAP.md` (alterado — T-206 marcada concluída, nota de conclusão)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-206, na ordem em que a sessão de fato seguiu):**
- ~~A tabela E-02 do `ROADMAP.md`... falta conferir T-207 especificamente~~ — **concluída na
  sequência desta mesma sessão**, ver abaixo.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-206):**
- **A cobertura não exaustiva é uma aposta calculada, não uma prova completa.** Os 10 relacionamentos
  e as ~11 tabelas ainda não exercitados individualmente compartilham mecanismo com os já testados,
  mas um erro de configuração específico de um deles (nome de constraint errado, tipo principal
  trocado) só apareceria se alguém tentasse de fato — o que só acontecerá organicamente quando T-3xx
  em diante começar a escrever nessas tabelas.
- **Nenhum risco novo de produto.** ADR-0004 item 9 já previa exatamente este mecanismo de
  verificação; esta tarefa amplia sua cobertura, não muda sua natureza.

---

**Interrupção fora do fluxo de tarefas:** entre o fechamento de T-206 e o início desta parte, um
pedido de "sim, segue com T-204" chegou — T-204 já estava concluída (commit `8a41b57`) duas tarefas
antes. Em vez de repetir trabalho já feito ou adivinhar a intenção, a sessão perguntou qual tarefa
era realmente pretendida (T-207, a única pendência real de E-02, ou T-301, início de E-03). A
resposta foi "CONTINUE", sem especificar — interpretada como "prossiga pela ordem do roadmap", que
aponta para T-207 como o próximo item ainda aberto de E-02, antes de abrir o épico seguinte.

**O que foi feito (T-207):**
- Conferência do que já existia: a coluna `purpose` e o enum `LaunchPurpose` (`UserInitiated` |
  `Prelaunch`) já tinham sido criados em T-202, junto com o resto do schema — não havia lacuna de
  modelo de dados, só a lacuna que o próprio critério de aceite de T-207 nomeia: o teste.
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/LaunchPurposeMeteringTests.cs` — grava 3
  lançamentos `UserInitiated` e 2 `Prelaunch` para o mesmo tenant/aplicativo/usuário, depois confirma
  que `Launches.CountAsync(l => l.Purpose == LaunchPurpose.UserInitiated)` devolve **3**, não **5**
  (o total real gravado) — a redação literal do critério de aceite ("contagem... não soma
  prelaunchs"), verificada contra PostgreSQL real, não assumida pela leitura do enum.
- **Decisão de escopo explícita, para não inventar requisito (RP-05)**: este teste não constrói nem
  simula um serviço de metering — RF-062 é MVP-1 (ADR-0006) e não tem endpoint nem consulta própria
  ainda. O teste prova a garantia na **camada de dados** que esse serviço, quando existir, vai
  depender — que é exatamente o que T-207 pede, nem mais nem menos.
- **29 testes automatizados no total** no Control Plane (7 Api + 22 Infrastructure), todos passando
  contra PostgreSQL real (reiniciado mais uma vez nesta sessão antes de rodar os testes — terceira
  vez, mesma causa já registrada em `docs/SETUP-DEV.md` §6).
- **E-02 · Fundação do Control Plane está com as 7 tarefas concluídas** (T-201 a T-207).

**Decisões tomadas (T-207):** nenhum ADR — execução de ADR-0016 Gap 1.

1. **Teste na camada de dados, não um serviço de metering fabricado** — construir uma consulta ou
   endpoint de metering só para ter algo a testar seria inventar escopo que nenhum documento aprovado
   pede agora; o critério de aceite fala em "contagem", que uma consulta LINQ/SQL já demonstra sem
   precisar de um serviço em volta dela.

**Arquivos criados/alterados (T-207):**
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/LaunchPurposeMeteringTests.cs` (criado)
- `docs/ROADMAP.md` (alterado — T-207 marcada concluída, nota de conclusão, E-02 fechado)
- `docs/STATUS.md` (alterado — §1, §3 — linha de Control Plane fechada, nova linha para E-03)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-207, na ordem em que a sessão de fato seguiu):**
- ~~**E-02 está completo.** O próximo épico é **E-03**, começando por **T-301**~~ — **T-301
  concluída na sequência desta mesma sessão**, ver abaixo. Antes disso, uma interjeição pediu
  explicitamente "T-204" (já concluída duas tarefas antes); a sessão perguntou qual tarefa era
  realmente pretendida em vez de adivinhar, e a resposta ("CONTINUE") foi interpretada como "siga
  a ordem do roadmap" — T-207 primeiro (única pendência real de E-02), depois T-301.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-207):**
- **Nenhum risco novo.** A garantia testada é de camada de dados; quando o serviço de metering real
  for construído (MVP-1), ele herda a garantia deste teste desde que use a mesma coluna e o mesmo
  filtro — não há acoplamento novo introduzido aqui que pudesse quebrar isso silenciosamente.

---

**O que foi feito (T-301, aprovada com "sim, segue com T-301"):**
- Leitura de `API.md` §2 (`POST /v1/auth/session`), do diagrama de sequência de login em
  `ARQUITETURA.md` §5.1 e de ADR-0001 (identidade) e ADR-0012 (convenções da API) antes de escrever
  qualquer código — para não inventar contrato que os documentos aprovados não davam.
- **Lacuna real encontrada durante a implementação**, não hipotética: `API.md` já promete
  `refreshToken` na resposta de login e um endpoint de refresh (T-303, "Token renova sem login;
  logout invalida"); nenhum documento decide onde esse refresh token seria guardado, e
  `MODELO-DE-DADOS.md` não tem tabela para isso. Sem persistência do lado do servidor, um JWT
  autocontido não pode ser revogado antes de expirar — e `logout` (RF-006) exige que a invalidação
  seja real. Por RP-07 (decisão relevante = ADR, antes da mudança) e RA-06 (mudança de modelo de
  dados exige ADR + changelog), a sessão escreveu **ADR-0017** antes de tocar em código, em vez de
  decidir isso silenciosamente dentro de uma classe C#.
- **ADR-0017** decide: `accessToken` em JWT, HMAC-SHA256, chave via `APPBRIDGE_JWT_SIGNING_KEY`
  (nunca arquivo, RP-06), claims `sub`/`tenant_id`/`upn`/`role`/`jti`, TTL de 15 min
  (`PREMISSA:` PRE-29); `refreshToken` opaco de 256 bits, **guardado só como hash SHA-256** — nunca
  o valor — em nova tabela `refresh_token` (TTL 30 dias, `PREMISSA:` PRE-30), com rotação no uso
  planejada para T-303. `MODELO-DE-DADOS.md` §4.3 amendado (mesmo padrão de ADR-0016 para o gap de
  `purpose`), com a tabela na rastreabilidade §13 e as duas premissas em §14.
- `src/AppBridge.ControlPlane.Domain/Identity/RefreshToken.cs` — `TenantScopedEntity`, não
  `AppendOnlyEntity`: `RevokedAt` precisa ser gravável depois da criação, o que uma tabela
  append-only proíbe por desenho (ADR-0011 §5).
- `Infrastructure/Configurations/RefreshTokenConfiguration.cs` — FK composta para `user_account`
  (mesmo padrão de T-204), índice único em `token_hash`. Migração `AddRefreshToken` gerada, revisada
  e aplicada a `appbridge_dev`.
- `Infrastructure/Identity/`: `IIdentityProvider`/`IdentityValidationResult` (a abstração que
  valida o token do provedor e resolve `ExternalSubject` + `Tenant.AdDomain` — **nenhuma
  implementação real nesta tarefa**, ver decisão 4 abaixo), `RefreshTokenHasher` (gera e faz hash do
  token opaco — compartilhado com o T-303 futuro, que vai fazer o mesmo hash para procurar o token
  apresentado), `ISessionTokenIssuer`/`JwtSessionTokenIssuer` (emissão pura do JWT, sem dependência
  de ASP.NET Core — testável isoladamente).
- `Api/Identity/DevIdentityProvider.cs` — aceita só `"dev:{externalSubject}:{adDomain}"`,
  deliberadamente não "qualquer coisa não vazia", para que um teste não confunda isso com
  comportamento permissivo por padrão. Registrado **somente sob `Development`**
  (`builder.Environment.IsDevelopment()`) — fora dela, `IIdentityProvider` não tem implementação
  nenhuma registrada, e a resolução de DI falha alto e cedo em vez de rodar sem verificação real.
- `Api/Endpoints/AuthEndpoints.cs` (`POST /v1/auth/session`) e `AuthProblems.cs` (Problem Details em
  português, código estável, conforme ADR-0012). Fluxo: valida identidade → resolve tenant por
  `AdDomain` (não filtrado por tenant, porque `Tenant` não é `ITenantScoped`) → **define
  `TenantContext.TenantId`** (o ponto que T-203 previu como "resolvido pelo middleware de T-301") →
  checa tenant suspenso → resolve `UserAccount` por `ExternalSubject` (agora sim filtrado por
  tenant) → checa usuário desabilitado → emite tokens e grava `access_event` +
  `refresh_token` numa única chamada a `IAuditWriter.ExecuteAsync`. **Toda tentativa, concedida ou
  negada, passa pelo mesmo `IAuditWriter`** — nenhum branch de negação grava por fora, para que
  nenhuma negação futura esqueça a garantia transacional do ADR-0007.
- `Program.cs`: `AppBridgeDbContext`, `TenantContext`/`ITenantContext`, `IAuditWriter`,
  `ISessionTokenIssuer` e (só em `Development`) `IIdentityProvider` registrados no contêiner de DI
  pela primeira vez — T-301 é o consumidor real que E-02 vinha preparando. `AddAuthentication().AddJwtBearer(...)`
  também registrado, sem nenhum endpoint `[Authorize]` para exercitá-lo ainda — mesmo raciocínio do
  health check de T-201: o mecanismo entra quando a decisão de claims é tomada, não quando o
  primeiro consumidor aparece. Health check ganhou `postgresql` como primeira dependência real.
- **Verificado rodando a aplicação de verdade** (`dotnet run` + `curl` + `psql`), não só nos testes:
  seedado um tenant e usuário reais em `appbridge_dev`, testados os cinco caminhos (sucesso, token
  inválido, usuário desconhecido em tenant conhecido, usuário desabilitado, tenant suspenso) e
  conferido no banco que `access_event`, `refresh_token` e `user_account.last_login_at` foram
  gravados exatamente como esperado — inclusive o caso do usuário desconhecido, que grava
  `access_event` com `user_account_id` nulo (o caso que `MODELO-DE-DADOS.md` §7.2 já previa).
  Dados de teste revertidos ao estado ativo/normal ao final.
- `tests/AppBridge.ControlPlane.Api.Tests/AuthEndpointTests.cs` — 6 testes contra o host real
  (`WebApplicationFactory`) e PostgreSQL real: sucesso grava tudo certo; token inválido não atribui
  a nenhum tenant; usuário desconhecido grava com `user_account_id` nulo; usuário desabilitado e
  tenant suspenso devolvem os códigos certos; e a falha de gravação simulada (mesma técnica de
  `SaveChangesInterceptor` de T-205) devolve `503 AUDIT_UNAVAILABLE` **na fronteira HTTP real**, não
  só no `AuditWriter` isolado.
- `tests/.../JwtSessionTokenIssuerTests.cs` — 2 testes sem banco: o token emitido valida com a
  mesma chave e claims corretos; falha com uma chave diferente.
- **Dois erros de teste encontrados e corrigidos, nenhum de produto:**
  1. As 3 suítes de T-201 (`HealthCheckTests`, `CorrelationIdMiddlewareTests`, `RequestLoggingTests`)
     quebraram porque `Program.cs` agora exige as duas variáveis de ambiente para iniciar — correto,
     é dependência real. Corrigido centralizando a configuração em `ApiTestFactory.cs`, reaproveitada
     pelos quatro arquivos de teste do projeto Api (em vez de repetir a configuração em cada um).
  2. A primeira versão de `ApiTestFactory` usava `ConfigureAppConfiguration` — não funcionou, porque
     `Program.cs` lê a configuração obrigatória **antes** de `Build()`, e esse hook do
     `WebApplicationFactory` só se aplica no momento em que ele intercepta `Build()`, tarde demais.
     Corrigido definindo variáveis de ambiente reais no processo, que `CreateBuilder(args)` já lê
     como uma de suas próprias fontes padrão, de forma síncrona — descoberto rodando a suíte, não
     por inspeção do código.
  3. (Achado à parte, no meio dos testes de `AuthEndpointTests`) O helper `NewDbContext()` das
     próprias verificações construía o contexto com `TenantContext` sem tenant — o filtro de leitura
     de T-203, funcionando exatamente como desenhado, cegava as próprias consultas de verificação do
     teste. Corrigido escopando o helper ao tenant do teste.
- **37 testes automatizados no total** no Control Plane (13 Api + 24 Infrastructure), todos passando
  contra PostgreSQL real. PostgreSQL local precisou ser reiniciado mais uma vez nesta sessão antes
  de rodar os testes — mesma causa já registrada em `docs/SETUP-DEV.md` §6.

**Decisões tomadas (T-301):** ADR-0017 (ver acima) cobre as decisões de maior peso. Decisões de
implementação menores, sem ADR próprio:

1. **Ordem de checagem: tenant suspenso antes de usuário desabilitado** — resolver o tenant já é
   necessário para tudo o mais (inclusive para saber a que tenant atribuir a auditoria); checar sua
   suspensão logo em seguida evita uma consulta de usuário desnecessária quando o tenant inteiro já
   está fora do ar.
2. **Todo caminho de negação passa por `IAuditWriter.ExecuteAsync`**, mesmo sem um "grant" real para
   proteger — ADR-0007 Parte 1 não distingue conceder de negar quanto à obrigação de registrar; um
   branch de negação que gravasse por fora do `AuditWriter` seria exatamente o tipo de exceção que a
   arquitetura de T-205 existe para não precisar.
3. **`DevIdentityProvider` aceita só um formato estrito** (`"dev:{subject}:{domain}"`), não qualquer
   string não vazia — para que nenhum teste (ou uso futuro acidental) confunda a ausência de
   verificação real com permissividade genérica.
4. **`IIdentityProvider` sem implementação real nesta tarefa** — decisão consciente, não lacuna:
   escrever uma integração Entra ID/AD DS sem nenhuma infraestrutura real para verificá-la
   produziria código não testável, o oposto da disciplina desta sessão de rodar para verificar.

**Arquivos criados/alterados (T-301):**
- `docs/adr/ADR-0017-token-de-sessao-e-persistencia-do-refresh-token.md` (criado)
- `docs/MODELO-DE-DADOS.md` (alterado — §4.3, §13, §14, cabeçalho de emendas)
- `src/AppBridge.ControlPlane.Domain/Identity/RefreshToken.cs` (criado)
- `src/AppBridge.ControlPlane.Infrastructure/AppBridgeDbContext.cs` (alterado — novo `DbSet`)
- `src/AppBridge.ControlPlane.Infrastructure/Configurations/RefreshTokenConfiguration.cs` (criado)
- `src/AppBridge.ControlPlane.Infrastructure/Migrations/20260810171012_AddRefreshToken.cs` e
  `.Designer.cs`, `AppBridgeDbContextModelSnapshot.cs` (criados/alterados)
- `src/AppBridge.ControlPlane.Infrastructure/Identity/*.cs` (6 arquivos, criados)
- `src/AppBridge.ControlPlane.Infrastructure/AppBridge.ControlPlane.Infrastructure.csproj` (alterado
  — pacote `System.IdentityModel.Tokens.Jwt`)
- `src/AppBridge.ControlPlane.Api/Identity/DevIdentityProvider.cs` (criado)
- `src/AppBridge.ControlPlane.Api/Endpoints/AuthEndpoints.cs`, `AuthProblems.cs` (criados)
- `src/AppBridge.ControlPlane.Api/HealthChecks/PostgresHealthCheck.cs` (criado)
- `src/AppBridge.ControlPlane.Api/Program.cs` (alterado — DI, `AddJwtBearer`, health check)
- `src/AppBridge.ControlPlane.Api/AppBridge.ControlPlane.Api.csproj` (alterado — referência a
  Infrastructure, pacote `Microsoft.AspNetCore.Authentication.JwtBearer`)
- `tests/AppBridge.ControlPlane.Api.Tests/ApiTestFactory.cs`, `AuthEndpointTests.cs`,
  `AssemblyInfo.cs` (criados)
- `tests/AppBridge.ControlPlane.Api.Tests/HealthCheckTests.cs`, `CorrelationIdMiddlewareTests.cs`,
  `RequestLoggingTests.cs` (alterados — usam `ApiTestFactory`)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/JwtSessionTokenIssuerTests.cs` (criado)
- `docs/ROADMAP.md` (alterado — T-301 marcada concluída, nota de conclusão, correção da nota de
  T-201 sobre PostgreSQL)
- `docs/STATUS.md` (alterado — §1, §3, §6 ADR, §7 premissas, §8 riscos)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-301, na ordem em que a sessão de fato seguiu):**
- ~~**T-302**, **T-303** e **T-304** são as próximas de E-03~~ — **T-303 concluída na sequência
  desta mesma sessão**, ver abaixo. T-302 e T-304 continuam pendentes.
- **`IIdentityProvider` real** (Entra ID/AD DS) continua sem implementação — bloqueado por E-01
  (nenhum hardware comprado, nenhum domínio, nenhum tenant Entra). Quando essa infraestrutura
  existir, é trabalho novo, não uma lacuna desta tarefa.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-301):**
- **R-032 (`STATUS.md` §8): `DevIdentityProvider` autentica sem verificação real.** Contido por só
  registrar sob `Development`, mas é o tipo de risco que precisa de atenção deliberada antes de
  qualquer configuração de deploy real — a mesma classe de cuidado de um `IgnoreQueryFilters()` mal
  colocado.
- **Os TTLs de token (PRE-29, PRE-30) não são medidos.** Mesma categoria de risco que PRE-07 já
  carrega para o `.rdp` — 15 min de acesso e 30 dias de refresh são escolhas de engenharia
  razoáveis, não validadas por uso real.
- **`AddAuthentication().AddJwtBearer()` está registrado sem nenhum endpoint que o exercite de
  ponta a ponta.** O teste de `JwtSessionTokenIssuerTests.cs` prova a emissão/validação do token de
  forma isolada, mas a integração completa com o pipeline HTTP real só será provada quando o primeiro
  endpoint `[Authorize]` existir (T-304 em diante).
- **Nenhum risco novo de produto além dos já registrados em ADR-0017.**

---

**O que foi feito (T-303, aprovada com "sim, segue com T-303"):**
- Leitura de `API.md` §2 (`/auth/refresh`, `/auth/logout`), `REQUISITOS.md` (RF-004..RF-006) e
  `MODELO-DE-DADOS.md` §7.2 (categorização de `access_event`) antes de escrever código.
- **Decisão de escopo explícita, para não inventar** (RP-05): o título de T-303 no `ROADMAP.md`
  mistura os dois endpoints de servidor com "armazenamento no Credential Manager" (RF-005) — que já
  é **T-803**, sob `E-08 · Launcher — fundação`, um projeto WinUI que não existe neste repositório
  (nada de E-04 a E-10 foi começado ainda; só E-01/E-02/E-03 parcial). Construir um launcher agora
  para "completar" o título seria escopo que ninguém pediu. T-303 ficou restrita ao servidor.
- `AuthProblems.cs` generalizado: cada método passou a receber `instance` explícito (antes fixo em
  `/v1/auth/session`), porque `INVALID_IDENTITY_TOKEN`/`USER_DISABLED`/`TENANT_SUSPENDED`/
  `AUDIT_UNAVAILABLE` agora são compartilhados entre três endpoints, e ADR-0012 §2 exige que
  `instance` reflita o caminho real da requisição. Novo `RefreshExpired` para o `401
  REFRESH_EXPIRED` que `API.md` já documentava.
- `POST /v1/auth/refresh`: busca o `refresh_token` pelo hash do valor apresentado — **segundo uso
  legítimo de `IgnoreQueryFilters()`** (o primeiro foi a resolução de tenant em T-301), pela mesma
  razão de bootstrap: a linha em si é a única coisa que identifica o tenant neste ponto, e ela É
  `ITenantScoped` (diferente de `Tenant`, que T-301 conseguia resolver sem a exceção). Confere
  validade/revogação/expiração (todas as três caem no mesmo `401 REFRESH_EXPIRED`, mesmo raciocínio
  anti-enumeração de ADR-0012 §5), reconfere `TenantStatus`/`UserAccountStatus` — **de novo**, não só
  no login, porque um refresh token em aberto para um usuário desabilitado depois de emitido não
  pode continuar renovando — e faz **rotação**: revoga o token apresentado no mesmo `SaveChanges` que
  grava o novo, então reutilizar um token já trocado (a assinatura de um roubo) passa a falhar a
  partir da primeira troca.
- **Decisão registrada, não esquecimento**: refresh **não passa por `IAuditWriter`**. ADR-0007 Parte
  1 lista os eventos bloqueantes por número de RF (RF-036, RF-037, RF-039, RF-041, RF-042) e RF-004
  não está entre eles; `MODELO-DE-DADOS.md` §7.2 categoriza `access_event` como cobrindo
  "autenticação (RF-036), logout, início e fim de sessão (RF-038)" — sem mencionar refresh. Inventar
  uma nova categoria de evento auditado sem nenhum documento pedir seria o mesmo erro, na direção
  oposta, que os Gaps 1/2 de ADR-0016 corrigiram (documentar de menos); aqui seria adicionar de mais.
- `POST /v1/auth/logout`: mesma busca por hash; se o token já não existe ou já está revogado, devolve
  `204` sem mais nada — idempotente por desenho, mesmo raciocínio anti-enumeração de não distinguir
  "nunca existiu" de "já foi usado". Quando encontra um token realmente ativo, **grava
  `access_event` (`logout`) via `IAuditWriter`** — este sim está na categorização de §7.2 — e revoga
  o token na mesma transação.
- **Bug real encontrado durante a verificação manual, não hipotético**: inspecionar
  `refresh_token.created_at` no `psql` mostrou `-infinity`. Investigação: `CreatedAt`/`UpdatedAt` são
  `init`-only em `AuditedEntity`/`AppendOnlyEntity` (imutabilidade de domínio, decisão de T-202), mas
  **nada em nenhum código, em nenhuma tarefa desde T-202, jamais os definia** — toda entidade
  inserida em qualquer teste ou endpoint desta sessão inteira gravou `DateTimeOffset.MinValue`
  silenciosamente, sem erro, porque `-infinity` é um valor válido de `timestamptz` que não quebra
  nada — só mente sobre quando a linha foi criada. Para um produto cuja tese inteira é auditabilidade
  (RA-07: "quem, o quê, **quando**, de onde"), isso não é um detalhe.
- **Corrigido no único lugar que resolve para sempre**, não em cada endpoint: `AppBridgeDbContext`
  ganhou um override de `SaveChanges`/`SaveChangesAsync` que percorre o `ChangeTracker` e carimba
  `CreatedAt` em toda entidade `Added` e `UpdatedAt` em toda `Modified`, usando
  `entry.Property(...).CurrentValue` — que consegue escrever uma propriedade `init` em tempo de
  execução porque o rastreador de mudanças do EF Core opera abaixo da restrição de tempo de
  compilação do C#, o mesmo mecanismo que já materializa entidade vinda do banco. Mesma disciplina de
  "mecanismo, não lembrete" de T-203 (filtro de tenant), T-204 (FK composta) e T-205 (`AuditWriter`).
- **Segundo bug encontrado no mesmo lote de verificação, em código já publicado por T-301**: nenhum
  DTO de requisição (`LoginRequest.IdentityToken`, e o novo `RefreshTokenRequest.RefreshToken`)
  exigia a presença do campo — tipo não-anulável em C# não é garantia de tempo de execução sem
  `required`; um corpo `{}` vinculava `null` silenciosamente, e a próxima linha (`identityToken.Split`)
  lançava `NullReferenceException`, virando um `500` genérico em vez do `400` correto. Corrigido
  marcando os dois campos `required` (não como parâmetro posicional do record — essa sintaxe não
  compila; como propriedade `required ... { get; init; }` dentro do corpo do record). Verificado
  enviando `{}` de propósito e confirmando `400 Bad Request`, não mais uma exceção não tratada.
- **Achado à parte, sem relação com código desta tarefa**: ao navegar `ROADMAP.md` para escrever esta
  mesma nota, faltava o cabeçalho `### E-04 · Catálogo — 11 pts` — removido sem querer pela edição
  que registrou a conclusão de T-301 (a âncora do texto substituído incluía a linha do título; o
  texto novo não a repôs). A tabela de T-401 a T-404 continuava presente, só sem o título da seção.
  Corrigido nesta sessão. Reforça revisar o `diff` de edições grandes em documentos longos, não só
  confiar que a âncora de um `Edit` preservou tudo ao redor dela.
- **Verificado rodando a aplicação de verdade** (`dotnet run` + `curl` + `psql`), a sessão completa:
  login → refresh (token novo emitido, antigo revogado) → reuso do token antigo recusado com
  `REFRESH_EXPIRED` → logout com o token atual → segundo logout idempotente (`204` de novo) →
  refresh depois do logout recusado → corpo malformado devolvendo `400` limpo. Conferido no banco
  que `refresh_token.created_at`/`access_event.created_at` deixaram de ser `-infinity`.
- `tests/AppBridge.ControlPlane.Api.Tests/RefreshLogoutEndpointTests.cs` — 9 testes, cada um fazendo
  login de verdade pelo endpoint real antes de exercitar refresh/logout (não monta token à mão):
  refresh válido rotaciona; reuso do token antigo falha; token desconhecido falha; usuário
  desabilitado e tenant suspenso falham no refresh; logout revoga e grava o `access_event`; logout
  invalida um refresh subsequente; logout idempotente não duplica o `access_event`; corpo malformado
  é `400`.
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/AuditColumnStampingTests.cs` — 3 testes:
  `CreatedAt` carimbado na inserção (e `UpdatedAt` continua nulo até uma atualização real);
  `UpdatedAt` carimbado na modificação sem mexer em `CreatedAt`; e o valor sobrevive a uma releitura
  real do PostgreSQL — deliberado, porque `-infinity` também "sobrevive" a uma releitura sem lançar
  exceção, então só ler de volta e checar a exceção não teria pego o bug original.
- **49 testes automatizados no total** no Control Plane (22 Api + 27 Infrastructure), todos passando
  contra PostgreSQL real.

**Decisões tomadas (T-303):** nenhum ADR — execução de RF-004/RF-006 e de ADR-0007/ADR-0012 já
decididos; correção de dois bugs, não decisão de arquitetura nova.

1. **Refresh não audita, logout audita** — a distinção não é arbitrária: seguiu exatamente a
   categorização que `MODELO-DE-DADOS.md` §7.2 já tinha (e que ADR-0007 Parte 1 já delimitava por
   número de RF), em vez de tratar os dois endpoints de forma simétrica só porque parecem
   semelhantes.
2. **Reconferir `TenantStatus`/`UserAccountStatus` a cada refresh**, não só no login — decisão de
   implementação pequena mas deliberada: sem isso, desabilitar uma conta não impediria sessões já
   abertas de continuar se renovando indefinidamente.
3. **Corrigir os dois bugs encontrados imediatamente**, no mesmo commit de T-303, em vez de abrir
   pendências — o de `CreatedAt`/`UpdatedAt` é sério o bastante (mente sobre "quando" numa trilha de
   auditoria) para não esperar; o de `required` é pequeno e mecânico o bastante para não valer a pena
   uma tarefa separada.

**Arquivos criados/alterados (T-303):**
- `src/AppBridge.ControlPlane.Api/Endpoints/AuthEndpoints.cs` (alterado — `Refresh`, `Logout`,
  `RefreshTokenRequest`, `SessionResponse`/`SessionUser`/`SessionTenant` renomeados de
  `LoginResponse`/`LoginUser`/`LoginTenant`, `LoginRequest`/`RefreshTokenRequest` com `required`)
- `src/AppBridge.ControlPlane.Api/Endpoints/AuthProblems.cs` (alterado — `instance` explícito,
  `RefreshExpired`)
- `src/AppBridge.ControlPlane.Infrastructure/AppBridgeDbContext.cs` (alterado — carimbo de
  `CreatedAt`/`UpdatedAt`)
- `tests/AppBridge.ControlPlane.Api.Tests/RefreshLogoutEndpointTests.cs` (criado)
- `tests/AppBridge.ControlPlane.Api.Tests/AuthEndpointTests.cs` (alterado — `SessionResponse`)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/AuditColumnStampingTests.cs` (criado)
- `docs/ROADMAP.md` (alterado — T-303 marcada concluída, nota de conclusão, cabeçalho `### E-04`
  restaurado)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-303):**
- **T-302** (vínculo identidade → conta AD por SID) e **T-304** (`AuthorizationService`) são as
  próximas de E-03.
- **`IIdentityProvider` real** continua sem implementação — mesma pendência de T-301, bloqueada por
  E-01.
- **T-803** (armazenamento no Windows Credential Manager) permanece em aberto, sob E-08, quando o
  launcher começar.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-303):**
- **`CreatedAt`/`UpdatedAt` incorretos desde T-202 é o achado mais sério desta sessão até aqui.**
  Ficou latente por quatro tarefas de implementação (T-202 a T-301) sem que nenhum teste o
  detectasse, porque nenhum teste anterior verificava especificamente o valor de `CreatedAt` — só a
  presença/ausência de linhas, ou outras colunas. Reforça o valor de inspecionar dado real no `psql`
  durante a verificação manual, não só confiar que "a query não lançou exceção" significa "o dado
  está certo".
- **O padrão de bug (propriedade nunca definida, sem erro em tempo de execução) pode se repetir.**
  Nenhuma outra coluna `init`-only foi auditada especificamente por este motivo nesta sessão — vale
  uma varredura dedicada antes do dogfood (T-005), não assumir que `CreatedAt`/`UpdatedAt` eram os
  únicos casos.
- **Nenhum risco novo de produto.** RF-004/RF-006 já prescreviam exatamente este comportamento.

---

**O que foi feito (T-304, aprovada com "sim, segue com T-304"):**
- Leitura de `REQUISITOS.md`/`ARQUITETURA.md` (RF-007, RF-021, RF-039, RNF-030) e do diagrama de
  sequência de lançamento em `ARQUITETURA.md` §5.2, que já documentava o componente
  `AuthorizationService` (`RF-007, RF-021, RF-039 | ADR-0004`) antes de escrever qualquer código.
- `SEGURANCA.md`, critério V-07: "Revogar acesso e cronometrar a negativa do lançamento seguinte
  (≤ 60 s) | RNF-030 | MVP-0". `MODELO-DE-DADOS.md` §5.2 confirmado: `application_permission` nunca
  é apagada, só tem `effective_to` fechado — revogação é vigência, não exclusão.
- **Escopo confirmado antes de codificar, por busca no `ROADMAP.md`** (`grep -n
  "permission\|Permiss"`): nenhuma outra tarefa constrói endpoint de concessão/revogação de
  permissão — T-304 é só o componente de decisão. `ApplicationPermission.cs` e
  `UserGroupMembership.cs` já existiam completos desde T-202/T-204, sem necessidade de schema ou
  migração nova.
- `IAuthorizationService`/`AuthorizationService`
  (`src/AppBridge.ControlPlane.Infrastructure/Authorization/`): um único método,
  `HasActivePermissionAsync(userAccountId, applicationId)`, devolvendo `bool` — deliberadamente sem
  enum de motivo de negação, porque um contrato mais rico só serviria ao futuro endpoint de
  lançamento (E-05), que ainda não existe; construí-lo agora seria escopo além do que T-304 pede
  (RP-05). Consulta um `join` entre `UserGroupMemberships` e `ApplicationPermissions` filtrando por
  `EffectiveFrom <= agora && (EffectiveTo == null || EffectiveTo > agora)`.
- **Isolamento entre tenants não é reimplementado**: ambos os `DbSet`s já são filtrados por tenant
  (ADR-0004, T-203), então uma consulta cruzando tenants simplesmente não encontra nada, sem código
  adicional — mecanismo já provado por T-203/T-206; deliberadamente não re-testado aqui, para não
  duplicar cobertura.
- **Por que nenhuma espera de relógio nos testes**: não há camada de cache entre a leitura de
  vigência e o banco — cada chamada relê `EffectiveFrom`/`EffectiveTo` direto. Isso torna o RNF-030
  (≤ 60 s) verdadeiro por construção: o teste que prova V-07 verifica "nega na checagem
  *imediatamente seguinte*" à revogação, não uma janela de tempo.
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/AuthorizationServiceTests.cs` — 6 testes:
  permissão dentro da janela concede; revogar nega a checagem seguinte (a prova literal de
  V-07/RNF-030 — checa antes e depois da revogação no mesmo teste, sem `Task.Delay`); nenhuma
  permissão nega; permissão de outro aplicativo não concede; permissão ainda não vigente
  (`EffectiveFrom` no futuro) nega; permissão já revogada no passado (`EffectiveTo` no passado) nega.
- `IAuthorizationService` registrado em `Program.cs`, ao lado de `IAuditWriter`/`ISessionTokenIssuer`
  (mesmo precedente de T-301) — mesmo sem consumidor real ainda.
- **Verificado subindo a aplicação de verdade** (`dotnet run`): confirma que a injeção de dependência
  resolve sem erro. Sem `curl` desta vez — não há endpoint `[Authorize]` que chame
  `IAuthorizationService`; isso só existe quando E-05 (`/launch`) for construído. Rodar a aplicação
  aqui prova só a fiação de DI, não um fluxo HTTP — registrado explicitamente, para não sugerir uma
  verificação mais forte do que a que de fato ocorreu.
- **Nenhum bug encontrado durante a verificação desta tarefa.**

**Decisões tomadas (T-304):** nenhum ADR — execução direta de RF-007/RF-021/RF-039/RNF-030 e do
componente já especificado em `ARQUITETURA.md` §5.2/ADR-0004; nenhuma escolha de arquitetura nova.

1. **Contrato mínimo (`bool`, sem motivo de negação)** — evita desenhar para o consumidor futuro
   (E-05) antes de ele existir; se o `/launch` precisar de mais detalhe (ex.: distinguir "sem
   permissão" de "permissão expirada" na resposta ao usuário), isso se decide quando E-05 começar,
   não agora.
2. **Sem camada de cache** — a alternativa (cache com TTL curto) tornaria RNF-030 uma aposta em
   afinação de TTL; ler direto do banco a cada chamada torna a garantia estrutural. O custo (uma
   consulta extra por lançamento) é aceito sem medição nesta tarefa — revisar se a carga real do
   dogfood (T-005) mostrar necessidade.
3. **Nenhum re-teste de isolamento entre tenants** — decisão de não duplicar cobertura que T-203/
   T-206 já garantem no nível do `DbContext`; testar de novo aqui seria repetir o mesmo mecanismo, não
   um comportamento novo de `AuthorizationService`.

**Arquivos criados/alterados (T-304):**
- `src/AppBridge.ControlPlane.Infrastructure/Authorization/IAuthorizationService.cs` (criado)
- `src/AppBridge.ControlPlane.Infrastructure/Authorization/AuthorizationService.cs` (criado)
- `src/AppBridge.ControlPlane.Api/Program.cs` (alterado — registro de `IAuthorizationService`)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/AuthorizationServiceTests.cs` (criado)
- `docs/ROADMAP.md` (alterado — T-304 marcada concluída, nota de conclusão)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-304):**
- **T-302** (vínculo identidade → conta AD por SID) é a última tarefa aberta de E-03.
- **Consumidor real de `IAuthorizationService`** não existe ainda — chega com E-05 (`/launch`), que
  também vai precisar de `IRdpFileSigner`, geração de `.rdp` assinado e `IAuditWriter` para o
  `access_event` de lançamento. Não é lacuna de T-304, é o próximo épico inteiro.
- **`IIdentityProvider` real** continua sem implementação — mesma pendência de T-301/T-303, bloqueada
  por E-01.
- **T-803** (armazenamento no Windows Credential Manager) permanece em aberto, sob E-08.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-304):**
- **Nenhum risco novo de produto.** RF-007/RF-021/RF-039/RNF-030 já prescreviam exatamente este
  comportamento; o componente só materializa o que já estava especificado.
- **Custo de consulta por lançamento não medido.** O `join` roda a cada chamada, sem cache — decisão
  2 acima assume que isso é barato o bastante; PRE-12 (catálogo p95 ≤ 300 ms) é o lugar certo para
  revisar isso quando houver carga real para medir (T-005).

---

**O que foi feito (T-302, aprovada com "sim, segue com T-302"):**
- Leitura de `REQUISITOS.md` (RF-002, RF-003), `ADR-0001` (o vínculo "identidade autenticada →
  conta AD DS", item 4: "o vínculo... já existe desde o primeiro dia") e `MODELO-DE-DADOS.md` §4.1
  (`ad_object_sid`: "SID, não `sAMAccountName`: sobrevive a renomeação") antes de escrever código.
- **Achado inicial, antes de decidir o desenho**: `ad_object_sid` existe na tabela desde T-202, é
  `required` na entidade, e a nota de por-que-SID já estava documentada — mas nenhum código em
  nenhuma tarefa anterior (T-301, T-303, T-304) jamais lia, verificava ou atualizava esse campo.
  Ele era só uma coluna. T-302 é a tarefa que faz o vínculo funcionar dentro do fluxo de login, não
  a que cria o campo (já existia).
- **Decisão de desenho, registrada por não ter ADR próprio** (é execução direta de ADR-0001, não
  uma decisão de arquitetura nova): a chave de resolução do usuário no login continua
  `ExternalSubject` (Entra `oid`) — não trocada para `AdObjectSid`. Razão: ADR-0001 já distingue os
  dois papéis ("quem autentica" vs. "qual conta abre a sessão"), e `ExternalSubject` já é estável a
  renomeação por natureza do Entra ID; misturar os dois papéis não foi pedido por nenhum requisito.
  O que uma renomeação de AD de fato muda são `Upn`/`DisplayName` (sAMAccountName/nome de exibição),
  não o SID — então é isso que precisa de sincronização, não a chave de busca.
- `IIdentityProvider.cs`: `IdentityValidationResult` ganhou `Upn`/`DisplayName` opcionais (com
  default `null`, para não quebrar nenhuma chamada existente) — os valores atuais do diretório,
  pensados para serem lidos frescos a cada validação, nunca em cache.
- `AuthEndpoints.cs`: novo método privado `SyncDirectoryAttributes(user, identity)`, chamado dentro
  do mesmo `grant` do `auditWriter.ExecuteAsync` que já grava `LastLoginAt` — atualiza
  `Upn`/`DisplayName` na linha já resolvida quando divergem do que o provedor de identidade
  devolveu, na mesma transação do login. **Nunca escreve `AdObjectSid`** — comentário explícito no
  código sobre por quê: é `required` na provisão (que este MVP ainda não tem endpoint para fazer,
  isso chega com E-04+) e é exatamente o campo que uma renomeação legítima não toca.
- **Decisão de não inventar um código de erro para divergência de SID**: cheguei a considerar negar
  o login se `AdObjectSid` do provedor divergisse do gravado (cenário de possível corrupção de
  diretório ou reatribuição de conta), mas `API.md` não documenta nenhum `appbridgeCode` para esse
  caso — adicionar um agora seria alterar o contrato de API sem que T-302 pedisse isso (RA-06). Field
  `AdObjectSid` não foi nem incluído em `IdentityValidationResult`, para não carregar um dado que
  nenhum comportamento usa (RP-05 na direção oposta: não só "não negar requisito", também "não
  carregar campo morto").
- `DevIdentityProvider.cs`: forma estendida de token,
  `dev:{externalSubject}:{adDomain}:{upn}:{displayName}` (`Split(':', 5)`), simulando uma leitura
  fresca do diretório sem alterar o comportamento do formato de três partes que T-301/T-303/T-304
  já usam em todos os seus testes (`parts.Length < 3` continua a única condição de invalidez).
- **Verificado rodando a aplicação de verdade** (`dotnet run` + `curl` + `psql`) o cenário exato do
  critério de aceite: tenant e usuário semeados via `psql`; primeiro login (`dev:oid-carla:...`);
  segundo login simulando renomeação (`dev:oid-carla:...:carla.rodrigues@...:Carla Rodrigues`);
  conferência direta no banco mostrando **uma única linha** de `user_account` (mesmo `id`),
  `ad_object_sid` inalterado, `upn`/`display_name` atualizados para os novos valores, e os **dois**
  `access_event` de login (antes e depois) apontando para o mesmo `user_account_id` — a trilha não
  quebrou. Dados de verificação limpos do banco `appbridge_dev` ao final.
- `tests/AppBridge.ControlPlane.Api.Tests/UserAccountSidLinkTests.cs` — 2 testes: renomear (segundo
  login com `Upn`/`DisplayName` novos) atualiza a mesma linha sem duplicar conta e mantém as duas
  `access_event` apontando para o mesmo `user_account_id`; um login com o token de forma curta
  (sem atributos de diretório) não altera o que já estava gravado — prova de não regressão
  deliberada, não só implícita pelos testes anteriores continuarem passando.
- **57 testes automatizados no total** no Control Plane (24 Api + 33 Infrastructure), todos passando
  contra PostgreSQL real. **Nenhum bug encontrado durante a verificação desta tarefa.**

**Decisões tomadas (T-302):** nenhum ADR — execução direta de RF-002/RF-003/ADR-0001; nenhuma
escolha de arquitetura nova, duas decisões de implementação registradas para não invenção de escopo.

1. **Chave de resolução do login permanece `ExternalSubject`, não `AdObjectSid`** — evita misturar
   os dois papéis que ADR-0001 já separa; nenhum requisito pede a troca.
2. **Nenhuma verificação/negação de divergência de SID construída** — `API.md` não documenta um
   código de erro para esse cenário; inventar um agora seria mudança de contrato de API não pedida
   por T-302 (RA-06). Fica registrado como possibilidade futura, não como lacuna desta tarefa.

**Arquivos criados/alterados (T-302):**
- `src/AppBridge.ControlPlane.Infrastructure/Identity/IIdentityProvider.cs` (alterado —
  `Upn`/`DisplayName` em `IdentityValidationResult`)
- `src/AppBridge.ControlPlane.Api/Identity/DevIdentityProvider.cs` (alterado — forma estendida de
  token)
- `src/AppBridge.ControlPlane.Api/Endpoints/AuthEndpoints.cs` (alterado — `SyncDirectoryAttributes`)
- `tests/AppBridge.ControlPlane.Api.Tests/UserAccountSidLinkTests.cs` (criado)
- `docs/ROADMAP.md` (alterado — T-302 marcada concluída, nota de conclusão, E-03 completo)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-302) — E-03 · Identidade e autorização está completo:**
- **Nenhuma tarefa aberta em E-03.** Próximo passo de produto são os épicos E-04 (Catálogo) e E-05
  (Lançamento), que ainda não começaram.
- **`IIdentityProvider` real** continua sem implementação — mesma pendência de todas as tarefas de
  E-03, bloqueada por E-01 (sem hardware, sem domínio AD DS, sem tenant Entra).
- **T-803** (armazenamento no Windows Credential Manager) permanece em aberto, sob E-08.
- **Consumidor real de `IAuthorizationService`** (T-304) segue pendente, junto com `/launch` — E-05.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-302):**
- **Nenhum risco novo de produto.** RF-002/RF-003/ADR-0001 já prescreviam exatamente este
  comportamento; a tarefa só materializa o que já estava especificado e documentado desde T-202.
- **Divergência de SID não tratada é uma lacuna consciente, não esquecida.** Se um `AdObjectSid`
  devolvido pelo provedor um dia divergir do gravado, o código de hoje simplesmente ignora a
  divergência (não atualiza, não nega, não registra). Isso é aceitável enquanto `IIdentityProvider`
  for só `DevIdentityProvider` (não há divergência real possível), mas precisa de decisão explícita
  antes de qualquer implementação real de Entra ID/AD DS — candidato natural a entrar no roteiro de
  verificações de segurança antes do dogfood (T-005), não pendência desta tarefa.

---

**O que foi feito (T-401, aprovada com "sim, segue com E-04" — primeira tarefa do épico):**
- **Nota de processo (RP-04)**: "segue com E-04" aprova a direção do épico, não uma única entrega —
  E-04 tem quatro tarefas (T-401..T-404). RP-04 continua "uma entrega por vez, termine, submeta à
  aprovação, só então avance" sem exceção prevista para aprovação por épico; segui com **apenas
  T-401** nesta rodada e reportei ao final, em vez de encadear T-402..T-404 na mesma resposta.
- Leitura de `REQUISITOS.md` (RF-012, RF-011, RF-013), `API.md` §3 (`GET /v1/applications`, já
  documentado por inteiro desde a fase de design) e `VISAO.md` §1/PA-01 (Domínio e Alterdata como os
  dois sistemas contábeis-alvo) antes de decidir o desenho do seed.
- **Decisão de desenho, sem ADR** (execução direta de RF-012, não escolha de arquitetura): seed
  grava direto nas tabelas via EF Core, sem arquivo JSON — RF-012 diz "JSON ou tabela" como formas
  alternativas de satisfazer "populado por seed, sem painel", não como exigência de as duas
  existirem; nada no repositório leria um arquivo JSON além do próprio seed, então criá-lo seria uma
  camada extra sem consumidor.
- **Decisão de desenho: CLI, não endpoint HTTP.** Cheguei a considerar uma rota
  `POST /admin/seed` para facilitar a operação, mas isso seria, na prática, o início do próprio
  painel administrativo que RF-012 diz que o MVP-0 não tem — esse painel é RF-043 (MVP-1). Um verbo
  de linha de comando (`dotnet run -- seed-catalog <ad-domain>`) antes de `app.Run()` em `Program.cs`
  resolve, sem abrir superfície HTTP nova.
- `CatalogSeeder.cs` (Infrastructure): idempotente por desenho — verifica existência por
  `RemoteAppAlias` + `HostPoolId` (mesmo escopo do índice único `uq_application_tenant_alias_pool`,
  MODELO-DE-DADOS.md §5.1) antes de inserir; cria o `HostPool` "Pool Principal" sob demanda. Seed
  fixo: Domínio Contábil e Alterdata, ambos `Status = Published` (para já aparecerem quando T-402
  existir).
- **Escopo deliberadamente restrito**: `IconRef` não é definido (isso é T-404, dono do endpoint de
  ícone); `ApplicationPermission`/`Group` não são criados (isso é T-402, que vai precisar desses
  dados como fixture do próprio teste de autorização — matéria de teste, não do seed de catálogo).
  RF-013 lista "grupo de permissão" entre os campos mínimos do catálogo, mas o critério de aceite
  literal de T-401 é "catálogo carregado sem painel" — os demais campos chegam com as tarefas que os
  consomem.
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/CatalogSeederTests.cs` — 2 testes: `SeedAsync`
  popula os dois aplicativos publicados, com `HostPoolId` válido, num único `HostPool`
  compartilhado; rodar `SeedAsync` duas vezes não duplica `application` nem `host_pool`.
- **Verificado rodando a aplicação de verdade** (`dotnet run -- seed-catalog <ad-domain>` + `psql`)
  contra `appbridge_dev`: tenant semeado via `psql`, comando executado duas vezes seguidas —
  confirmado no banco que só existem as 2 linhas de `application` e 1 de `host_pool` esperadas, sem
  duplicação. Tenant inexistente testado à parte, sem pipe no meio (a primeira tentativa usou
  `| tail -2; echo $?`, que capturava o código de saída do `tail`, não o do `dotnet run` — corrigido
  redirecionando para arquivo antes de checar `$?`, evitando reportar `EXIT: 0` de um comando que na
  verdade tinha devolvido `1`). Dados de verificação limpos do banco ao final.
- **57 testes anteriores continuam passando** + **2 novos = 59 testes automatizados no total** (24
  Api + 35 Infrastructure). **Nenhum bug encontrado no código** — o único desvio foi na minha própria
  verificação manual (o `$?` após pipe), corrigido antes de confirmar o resultado.

**Decisões tomadas (T-401):** nenhum ADR — execução direta de RF-012; duas decisões de
implementação registradas para não invenção de escopo.

1. **Seed grava direto nas tabelas, sem arquivo JSON** — nada consumiria um arquivo separado; manter
   um formato que ninguém lê seria complexidade sem função.
2. **Acionado por CLI, não por endpoint HTTP** — um endpoint de seed seria, de fato, o começo do
   painel administrativo que RF-012 explicitamente diz que este MVP não tem.

**Arquivos criados/alterados (T-401):**
- `src/AppBridge.ControlPlane.Infrastructure/Catalog/CatalogSeeder.cs` (criado)
- `src/AppBridge.ControlPlane.Api/Program.cs` (alterado — verbo de CLI `seed-catalog`)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/CatalogSeederTests.cs` (criado)
- `docs/ROADMAP.md` (alterado — T-401 marcada concluída, nota de conclusão)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-401):**
- **T-402** (`GET /applications` filtrado por autorização) é a próxima de E-04 — depende do catálogo
  semeado (T-401, pronta) e de `IAuthorizationService` (T-304, pronta), mas ainda não tem aprovação
  explícita para começar (RP-04 — ver nota de processo acima).
- **T-403** (`ETag`/`If-None-Match`) e **T-404** (endpoint de ícone) seguem T-402 na ordem natural do
  épico.
- **`IIdentityProvider` real**, **T-803** e **consumidor real de `IAuthorizationService`** continuam
  as mesmas pendências já registradas em tarefas anteriores desta sessão.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-401):**
- **Nenhum risco novo de produto.** RF-012 já prescrevia exatamente este comportamento.
- **O dataset do seed é fixo em código.** Se o dogfood precisar de um terceiro aplicativo antes de
  T-404/painel existir, hoje isso exige editar `CatalogSeeder.cs` e recompilar — aceitável para o
  escopo "sem painel" do MVP-0, mas vale registrar como limitação deliberada, não descoberta depois.

---

**Nota de processo — ambiguidade na aprovação.** A mensagem seguinte do Frederico foi "sim, segue
com T-302", mas T-302 já estava concluída e enviada nesta mesma sessão (commit `88e55da`) — a
resposta anterior minha tinha proposto T-402 como próxima tarefa. Em vez de assumir a leitura mais
provável (erro de digitação) ou pedir para refazer T-302 sem confirmar, usei `AskUserQuestion`
apresentando as duas leituras possíveis; a resposta foi "você decide". Decidi por T-402, por ser a
tarefa que eu mesmo tinha acabado de propor como próxima e por T-302 não ter nenhum motivo
registrado para reabertura. Registro isso explicitamente porque RP-05 pede pergunta na dúvida, mas
também porque seguir sem confirmar teria arriscado refazer trabalho já entregue.

**O que foi feito (T-402):**
- Leitura de `API.md` §3 (`GET /v1/applications`, já documentado por inteiro desde a fase de
  design), `REQUISITOS.md` (RF-011, RF-013, RF-015) e `ARQUITETURA.md` §4 (diagrama de componentes:
  `CatalogService --> AuthorizationService`) antes de decidir o desenho.
- **Escopo restrito à linha literal do `ROADMAP.md`**: só a listagem `GET /v1/applications`
  filtrada por autorização. `ETag`/`If-None-Match` (RF-015) é T-403; o endpoint de ícone (PD-03) é
  T-404; `GET /v1/applications/{id}` (detalhe) não tem tarefa própria no roadmap — `API.md` já o
  documenta na mesma seção, mas documentar não obriga implementar tudo de uma seção numa única
  tarefa (mesmo raciocínio de T-301 não ter implementado `/refresh` só porque `API.md` já o
  descrevia; lá quem implementou foi T-303).
- **Achado antes de escrever código**: nenhuma peça de infraestrutura existia ainda para popular
  `TenantContext.TenantId` a partir de um token de sessão já emitido — toda rota anterior resolvia o
  tenant consultando o banco dentro do próprio handler (login por `Tenant.AdDomain`; refresh/logout
  pelo hash do token). `GET /v1/applications` é a primeira rota cuja única fonte de tenant é a claim
  `tenant_id` que `JwtSessionTokenIssuer` já grava no token desde T-301 (ADR-0017 §1) — o comentário
  em `TenantContext.cs`/`ITenantContext.cs` já previa isso ("T-301's auth middleware sets it early
  in the pipeline"), mas nenhuma tarefa antes de T-402 tinha uma rota que precisasse dele de
  verdade.
- `src/AppBridge.ControlPlane.Api/Middleware/TenantResolutionMiddleware.cs` (criado): lê
  `context.User.FindFirst("tenant_id")` e carimba `TenantContext.TenantId`, registrado em
  `Program.cs` entre `UseAuthentication()` e `UseAuthorization()` — depois de saber quem está
  autenticado, antes de qualquer consulta com filtro de tenant rodar.
- `IAuthorizationService.GetAuthorizedApplicationIdsAsync` (nova, em `IAuthorizationService.cs`/
  `AuthorizationService.cs`): forma em lote de `HasActivePermissionAsync` (T-304), reaproveitando a
  mesma janela de vigência (`EffectiveFrom`/`EffectiveTo`) via a mesma consulta de junção — decisão
  deliberada de não duplicar essa lógica no endpoint, seguindo o próprio diagrama de componentes
  (`CatalogService --> AuthorizationService`).
- `src/AppBridge.ControlPlane.Api/Endpoints/CatalogEndpoints.cs` (criado): `GetApplications`
  extrai `sub` do token, busca IDs autorizados via `IAuthorizationService`, filtra
  `Applications.Where(a => a.Status == Published && authorizedIds.Contains(a.Id))`, monta a resposta
  no formato exato de `API.md` §3. `available` sempre `true` — não existe hoje nenhuma fonte de
  saúde de host (`GetHostHealthAsync` é V2, `ARQUITETURA.md` §4.2) para popular esse campo com
  qualquer outro valor; inventar uma seria escopo além do que T-402 pede.
- **Bug real encontrado durante a verificação manual — na minha própria semeadura via `psql`, não no
  código**: a primeira tentativa de popular `application`/`group`/`user_group_membership`
  manualmente usou literais numéricos (`status=1`, `launch_mode=0`, `source=0`), tratando essas
  colunas como se fossem inteiras. `\d application` revelou que `status` é `text` — o EF Core
  converte esses enums para string minúscula (`'published'`, `'remote_app'`, `'local'`), não para o
  índice numérico. O valor gravado pela minha semeadura foi literalmente a string `"1"`, que nunca
  bate com `a.status = 'published'` na consulta real gerada pelo EF Core — o endpoint respondia
  `200` com `items: []` mesmo com a permissão certa concedida, porque o filtro de status excluía
  silenciosamente a linha. Diagnosticado comparando o SQL efetivamente executado (do log
  estruturado, que já mostrava `a.status = 'published'` com um `@authorizedApplicationIds1` único —
  ou seja, a junção de autorização **já tinha encontrado** o aplicativo certo) com o valor real da
  coluna via `\d application` + `SELECT status`. Corrigido a mão no dado de verificação, não no
  código — os testes automatizados nunca passariam por esse valor errado, porque usam
  `DbContext.Applications.Add(new Application { Status = ApplicationStatus.Published })`, que já
  grava a string certa através do conversor de valor do EF Core.
- **Também descartado durante a mesma investigação**: uma colisão de UUID na minha primeira
  tentativa de semear dados fixos (`11111111-...`) com uma linha pré-existente no banco
  `appbridge_dev` ("Escritório Modelo", de uma sessão anterior não relacionada a esta tarefa) —
  ambos os `INSERT`s falharam por chave duplicada e não gravaram nada; não toquei na linha
  pré-existente (não é minha para apagar) e troquei para `gen_random_uuid()` na semeadura seguinte,
  que teve sucesso.
- **Verificado rodando a aplicação de verdade** (`dotnet run` + `curl` + `psql`): tenant, usuário,
  host pool, aplicativo, grupo, vínculo e permissão semeados via `psql` (com o valor de enum
  corrigido); login real; `GET /v1/applications` sem token → `401`; com token → `Domínio Contábil`
  aparece com os campos exatos de `API.md` §3, `Alterdata` (nunca autorizada) não aparece. Dados de
  verificação limpos do banco ao final.
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/AuthorizationServiceTests.cs` — 3 novos testes
  para `GetAuthorizedApplicationIdsAsync`: devolve só o aplicativo com permissão vigente; exclui um
  já revogado; usuário sem nenhum vínculo de grupo devolve conjunto vazio.
- `tests/AppBridge.ControlPlane.Api.Tests/CatalogEndpointTests.cs` (criado) — 5 testes: sem token →
  `401`; autorizado e publicado aparece com os campos certos; não autorizado não aparece; autorizado
  mas `Draft` não aparece; token de um tenant nunca vê aplicativo autorizado de outro tenant (dois
  tenants completos, dois usuários, isolamento provado de ponta a ponta pela pilha HTTP real, não só
  pelo filtro do `DbContext`).
- **67 testes automatizados no total** no Control Plane (29 Api + 38 Infrastructure), todos
  passando contra PostgreSQL real.

**Decisões tomadas (T-402):** nenhum ADR — execução direta de RF-011/RF-013/ARQUITETURA.md §4;
três decisões de implementação registradas para não invenção de escopo.

1. **`TenantResolutionMiddleware` lê a claim `tenant_id`, não reconsulta o banco** — a claim já
   existe no token desde T-301; reconsultar seria redundante, não mais correto.
2. **`IAuthorizationService` ganha um segundo método em vez do endpoint reimplementar a consulta de
   vigência** — segue o próprio diagrama de componentes (`CatalogService --> AuthorizationService`)
   e evita duas cópias da mesma janela `EffectiveFrom`/`EffectiveTo`.
3. **`GET /v1/applications/{id}` não foi construído** — não tem tarefa própria no roadmap; `API.md`
   documentá-lo na mesma seção de `GET /v1/applications` não é mandato para implementá-lo junto.

**Arquivos criados/alterados (T-402):**
- `src/AppBridge.ControlPlane.Api/Middleware/TenantResolutionMiddleware.cs` (criado)
- `src/AppBridge.ControlPlane.Api/Endpoints/CatalogEndpoints.cs` (criado)
- `src/AppBridge.ControlPlane.Api/Program.cs` (alterado — middleware e `MapCatalogEndpoints()`)
- `src/AppBridge.ControlPlane.Infrastructure/Authorization/IAuthorizationService.cs` (alterado —
  `GetAuthorizedApplicationIdsAsync`)
- `src/AppBridge.ControlPlane.Infrastructure/Authorization/AuthorizationService.cs` (alterado)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/AuthorizationServiceTests.cs` (alterado)
- `tests/AppBridge.ControlPlane.Api.Tests/CatalogEndpointTests.cs` (criado)
- `docs/ROADMAP.md` (alterado — T-402 marcada concluída, nota de conclusão)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-402):**
- **T-403** (`ETag`/`If-None-Match`) é a próxima de E-04 — depende de `GET /v1/applications`
  (T-402, pronta).
- **T-404** (endpoint de ícone) segue T-403 na ordem natural do épico.
- **`GET /v1/applications/{id}`** continua sem tarefa própria — registrado, não esquecido; entra se
  e quando algo pedir (por exemplo, T-405+ ou o próprio launcher).
- **`IIdentityProvider` real**, **T-803** e **consumidor real de `HasActivePermissionAsync`** (o
  `/launch` de E-05) continuam as mesmas pendências já registradas em tarefas anteriores.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-402):**
- **Nenhum risco novo de produto.** RF-011/RF-013 já prescreviam exatamente este comportamento.
- **Literais numéricos em seed manual fora do EF Core são uma armadilha real, não hipotética** — já
  aconteceu nesta própria sessão. Qualquer script de seed manual futuro (fora de `CatalogSeeder` ou
  do `DbContext`) precisa usar os valores de string exatos que o conversor de enum grava, não o
  índice numérico do enum C#. Vale uma nota em `docs/SETUP-DEV.md` se esse tipo de seed manual via
  `psql` se repetir com frequência.
- **`available` é sempre `true`** — nenhuma fonte de saúde de host existe para popular esse campo de
  outra forma; aceitável para MVP-0, mas o campo silenciosamente não significa "operacional
  confirmado" até `GetHostHealthAsync` (V2) existir.

---

**O que foi feito (T-403, aprovada com "sim, segue com T-403"):**
- Leitura de `API.md` §3 (o exemplo `ETag: "cat-018f3a92"` e a frase "com `If-None-Match` e catálogo
  inalterado, responde `304 Not Modified` sem corpo") antes de decidir o desenho.
- **Decisão de desenho, sem ADR** (execução direta de RF-015): o `ETag` é um hash de conteúdo sobre
  a própria lista de itens já materializada para o usuário (`SHA256` truncado a 16 hex, prefixo
  `cat-`), não um contador de versão mantido à parte. Considerei versionar `Application.UpdatedAt`
  (já carimbado desde a correção de T-303) como base do `ETag`, mas isso ignoraria mudanças de
  `ApplicationPermission` — o catálogo de um usuário muda tanto quando um `Application` é editado
  quanto quando a permissão dele é concedida/revogada, e um `ETag` baseado só em `UpdatedAt` de
  `Application` ficaria desatualizado exatamente no caso mais comum (conceder/revogar acesso). Hash
  do resultado final materializado captura os dois motivos de mudança de graça, porque é literalmente
  o que seria enviado ao cliente.
- `If-None-Match` comparado por igualdade de string exata (mais o caso trivial `*`) contra o `ETag`
  calculado — sem suporte a validadores fracos (`W/"..."`), não pedido por `API.md` e o hash de
  conteúdo já é, por natureza, um validador forte.
- `CatalogEndpoints.GetApplications` alterado: computa o `ETag` sobre `items` já montado, grava o
  cabeçalho de resposta sempre (`200` e `304` igual — `304` também carrega `ETag`, prática padrão de
  HTTP), e só então decide devolver `304` (sem corpo) ou `200` (com o corpo de sempre).
- **Verificado rodando a aplicação de verdade** (`dotnet run` + `curl`): primeira requisição devolve
  `200` com `ETag: "cat-4fdb3c35dee7d96f"` e o corpo esperado; segunda requisição com o mesmo
  `If-None-Match` devolve `304 Not Modified`, mesmo `ETag`, e **corpo de 0 bytes** — conferido com
  `curl -w "%{size_download}"`, não só olhando o status.
- `tests/AppBridge.ControlPlane.Api.Tests/CatalogEndpointTests.cs` — 3 novos testes: primeira
  requisição devolve `ETag` e a segunda idêntica devolve `304`; `If-None-Match` desatualizado (um
  valor arbitrário que nunca correspondeu a nada) devolve `200` com o catálogo atual; conceder uma
  nova permissão — sem tocar em nenhuma linha de `Application` — muda o `ETag` e o `If-None-Match`
  antigo volta a devolver `200` com os dois itens. Este último é a prova direta da decisão de
  desenho: se o `ETag` fosse baseado só em `Application.UpdatedAt`, esse teste falharia.
- **70 testes automatizados no total** no Control Plane (32 Api + 38 Infrastructure), todos
  passando contra PostgreSQL real. **Nenhum bug encontrado durante a verificação desta tarefa** —
  primeira tarefa da sessão sem achado de código nem de semeadura manual.

**Decisões tomadas (T-403):** nenhum ADR — execução direta de RF-015 e do exemplo já documentado em
`API.md` §3; uma decisão de implementação registrada para não repetir o erro que quase cometi.

1. **`ETag` como hash do resultado materializado, não de `Application.UpdatedAt`** — a alternativa
   mais óbvia (versionar só pela última modificação da tabela `Application`) ficaria errada
   silenciosamente toda vez que o motivo da mudança fosse permissão, não conteúdo — exatamente o
   caso mais comum de "o catálogo do usuário mudou" neste produto.

**Arquivos criados/alterados (T-403):**
- `src/AppBridge.ControlPlane.Api/Endpoints/CatalogEndpoints.cs` (alterado — `ETag`/`If-None-Match`)
- `tests/AppBridge.ControlPlane.Api.Tests/CatalogEndpointTests.cs` (alterado)
- `docs/ROADMAP.md` (alterado — T-403 marcada concluída, nota de conclusão)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-403) — E-04 fica só com T-404 em aberto:**
- **T-404** (endpoint de ícone, resolve PD-03) é a última tarefa de E-04.
- **`GET /v1/applications/{id}`** continua sem tarefa própria — mesma pendência registrada em T-402.
- **`IIdentityProvider` real**, **T-803** e **consumidor real de `HasActivePermissionAsync`** (o
  `/launch` de E-05) continuam as mesmas pendências já registradas em tarefas anteriores.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-403):**
- **Nenhum risco novo de produto.** RF-015 já prescrevia exatamente este comportamento.
- **O hash é recalculado a cada requisição, sem cache** — o custo é uma serialização de string curta
  mais um `SHA256` sobre poucos itens (dezenas, não milhares, no MVP-0); aceitável sem medição
  agora, mesma categoria de PRE-12 se algum dia precisar ser revisado.

---

**O que foi feito (T-404, aprovada com "sim, segue com T-404" — última tarefa de E-04):**
- Leitura de `API.md` §3 (`GET /v1/applications/{id}/icon`, "o ícone é servido pelo Control Plane a
  partir de armazenamento de arquivos, referenciado por `icon_ref`; não fica no banco") e
  `MODELO-DE-DADOS.md` §14/PD-03 antes de decidir o desenho.
- **PD-03 já estava "✅ Resolvida" em `API.md`** (decisão de fase de design: sistema de arquivos, não
  banco), mas nenhuma peça de código existia para isso — T-404 é a implementação dessa decisão já
  tomada, não uma decisão nova. Sem ADR: execução de RF-013 e do que `API.md` já documentava.
- `src/AppBridge.ControlPlane.Infrastructure/Catalog/IIconStorage.cs` +
  `FileSystemIconStorage.cs` (criados): resolve `icon_ref` para bytes sob um diretório raiz
  configurado via `IconStorageOptions.RootPath` (`APPBRIDGE_ICON_STORAGE_PATH`).
- **Bug pego em auto-revisão antes de rodar qualquer teste, não durante a verificação manual**: a
  primeira versão do guarda contra travessia de caminho comparava `candidate.StartsWith(root)` sem
  separador — um diretório irmão que começasse com os mesmos caracteres do `root` (`root="/a/b"`,
  fuga para `/a/bc/evil`) passaria pelo teste de prefixo indevidamente. Corrigido acrescentando o
  separador de diretório ao final de `root` antes da comparação. Registrado porque é o tipo de erro
  que só um teste dedicado de travessia (que escrevi logo depois) provaria — e eu quase não tinha
  escrito esse teste antes de notar o problema relendo o código.
- `IIconStorage`/`FileSystemIconStorage` registrados em `Program.cs` — variável de ambiente
  `APPBRIDGE_ICON_STORAGE_PATH` obrigatória, mesmo padrão de `APPBRIDGE_DB_CONNECTION`/
  `APPBRIDGE_JWT_SIGNING_KEY` (falha rápido, documentado, nunca lido em silêncio).
- `CatalogEndpoints.cs`: nova rota `GET /v1/applications/{id}/icon`, `[Authorize]`. Busca
  `Application` pelo `id` (filtro de tenant automático, ADR-0004 — `id` de outro tenant não é
  encontrado, `404` igual a inexistente, mesmo raciocínio anti-enumeração de ADR-0012 §5), lê o
  arquivo via `IIconStorage`, calcula `ETag` de conteúdo (reaproveitando o mesmo `HashHex` de T-403,
  fatorado para os dois usos) e grava `Cache-Control: public, max-age=604800, immutable`.
- **Decisão de desenho: sem filtro de autorização (RF-011) neste endpoint** — considerei replicar o
  filtro de `GetAuthorizedApplicationIdsAsync` aqui também, mas um ícone é metadado de apresentação,
  não o aplicativo em si, e a seção de `API.md` que documenta este endpoint não pede o filtro que
  `GET /v1/applications` aplica à listagem (RF-011 é citado só na seção da listagem). Construí-lo
  mesmo assim seria escopo além do que T-404 pede — se um requisito futuro pedir, é mudança de
  escopo com ADR/changelog próprios, não algo a antecipar aqui.
- **`CatalogSeeder` (T-401) atualizado**: `IconRef` preenchido para os dois aplicativos do dogfood —
  deixado em aberto de propósito em T-401 ("isso é T-404, dono do endpoint de ícone"). Dois PNGs
  placeholder 64×64 gerados nesta sessão (`assets/catalog-icons/dominio-contabil.png`,
  `alterdata.png`), construídos por codificação manual dos chunks PNG (assinatura + `IHDR` + `IDAT`
  comprimido por `zlib` + `IEND`) via `python3`, sem Pillow disponível neste ambiente
  (`ModuleNotFoundError` ao tentar `from PIL import Image`) e sem instalar nada novo. Cores sólidas
  distintas (azul, verde) só para diferenciar visualmente os dois — não são a identidade visual
  final, decisão de produto para quando o painel (RF-043, MVP-1) existir.
- `docs/SETUP-DEV.md` (alterado): nova variável documentada, nova §6 "Semear o catálogo (dev)" com o
  comando completo (`dotnet run -- seed-catalog <ad-domain>`) e as três variáveis que ele exige
  mesmo sem usar todas — `Program.cs` as lê antes de decidir qual caminho de execução seguir.
- `tests/AppBridge.ControlPlane.Api.Tests/ApiTestFactory.cs` (alterado): terceira variável de
  ambiente obrigatória (`APPBRIDGE_ICON_STORAGE_PATH`) — autocontida, apontando para um diretório
  temporário próprio por execução com os dois PNGs do seed escritos nele (um PNG 1×1 mínimo bem
  conhecido, não os placeholders reais — o teste não precisa deles serem os mesmos arquivos, só
  precisa deles existirem e serem PNGs válidos), em vez de depender do diretório de trabalho do
  processo de teste coincidir com o layout do repositório. `Dispose(bool)` sobrescrito para apagar o
  diretório temporário ao final.
- **Verificado rodando a aplicação de verdade** (`dotnet run -- seed-catalog` + `curl` + `diff`):
  tenant semeado, catálogo semeado via CLI (agora com `IconRef`), usuário/grupo/permissão via
  `psql` (com os literais de enum corretos desta vez — lição de T-402 aplicada de propósito);
  login; `GET /v1/applications` confirma `iconUrl`; `GET .../icon` devolve `200`,
  `Content-Type: image/png`, `Cache-Control: public, max-age=604800, immutable`, `ETag`, e os bytes
  batem **byte a byte** (`diff`) com o arquivo original em disco; segunda requisição com o mesmo
  `If-None-Match` devolve `304` com 0 bytes; `id` desconhecido devolve `404`. Dados de verificação
  limpos do banco ao final.
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/FileSystemIconStorageTests.cs` (criado) — 4
  testes, sem PostgreSQL (comportamento puro de sistema de arquivos): lê bytes de um arquivo sob a
  raiz; devolve `null` para arquivo inexistente; recusa travessia de caminho relativa
  (`../arquivo`) e absoluta (caminho fora da raiz passado diretamente) — os dois últimos provam a
  correção do bug pego em auto-revisão.
- `tests/AppBridge.ControlPlane.Api.Tests/CatalogIconEndpointTests.cs` (criado) — 7 testes: sem
  token → `401`; serve PNG com `ETag`/`Cache-Control` corretos; segunda requisição idêntica → `304`
  sem corpo; aplicativo sem `IconRef` → `404`; `IconRef` que não resolve a arquivo → `404`; `id`
  desconhecido → `404`; token de outro tenant não lê o ícone de uma aplicação de outro tenant →
  `404`.
- **81 testes automatizados no total** no Control Plane (39 Api + 42 Infrastructure), todos
  passando contra PostgreSQL real (mais os 4 de `FileSystemIconStorageTests.cs`, que não precisam
  dele). **Nenhum bug de produção encontrado durante a verificação manual** — o único bug real desta
  tarefa (o guarda de travessia sem separador) foi pego em auto-revisão antes de qualquer teste
  rodar, não durante a verificação.

**Decisões tomadas (T-404):** nenhum ADR — execução direta de RF-013 e da decisão de PD-03 já
registrada em `API.md`; duas decisões de implementação registradas para não invenção de escopo.

1. **Sem filtro de autorização (RF-011) no endpoint de ícone** — a seção de `API.md` que o documenta
   não pede; construí-lo seria antecipar um requisito que não foi pedido.
2. **Ícones placeholder gerados nesta sessão, não a identidade visual real** — decisão de produto
   que cabe a Frederico quando o painel (RF-043) existir; o MVP-0 só precisa do mecanismo
   funcionando, não do resultado final.

**Arquivos criados/alterados (T-404):**
- `src/AppBridge.ControlPlane.Infrastructure/Catalog/IIconStorage.cs` (criado)
- `src/AppBridge.ControlPlane.Infrastructure/Catalog/FileSystemIconStorage.cs` (criado)
- `src/AppBridge.ControlPlane.Infrastructure/Catalog/CatalogSeeder.cs` (alterado — `IconRef`)
- `src/AppBridge.ControlPlane.Api/Endpoints/CatalogEndpoints.cs` (alterado — rota de ícone)
- `src/AppBridge.ControlPlane.Api/Program.cs` (alterado — registro de `IIconStorage`)
- `tests/AppBridge.ControlPlane.Api.Tests/ApiTestFactory.cs` (alterado — terceira variável)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/FileSystemIconStorageTests.cs` (criado)
- `tests/AppBridge.ControlPlane.Api.Tests/CatalogIconEndpointTests.cs` (criado)
- `assets/catalog-icons/dominio-contabil.png` (criado)
- `assets/catalog-icons/alterdata.png` (criado)
- `docs/SETUP-DEV.md` (alterado — §3 e nova §6)
- `docs/ROADMAP.md` (alterado — T-404 marcada concluída, nota de conclusão, E-04 completo)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-404) — E-04 · Catálogo está completo:**
- **Nenhuma tarefa aberta em E-04.** Próximo épico de produto é **E-05 · Lançamento** — depende de
  `IAuthorizationService` (T-304, pronta), mas também de `IRdpFileSigner` (ADR-0009) e
  `ISessionBackend` (ARQUITETURA.md §4.2), nenhum dos dois construído ainda.
- **`GET /v1/applications/{id}`** (detalhe) continua sem tarefa própria — mesma pendência registrada
  em T-402/T-403.
- **`IIdentityProvider` real** e **T-803** continuam as mesmas pendências já registradas em tarefas
  anteriores desta sessão.
- **Identidade visual real dos ícones** — decisão de produto de Frederico, não bloqueante para o
  MVP-0.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-404):**
- **Nenhum risco novo de produto.** RF-013 já prescrevia exatamente este comportamento; PD-03 já
  estava resolvida na documentação.
- **`icon_ref` só é confiável enquanto só `CatalogSeeder` o escreve.** Se um futuro endpoint de
  publicação (RF-043, MVP-1) permitir que um administrador digite `icon_ref` livremente, a proteção
  de travessia de caminho desta tarefa passa a ser a única linha de defesa real (hoje é defesa em
  profundidade contra um cenário que ainda não existe) — vale revisar quando esse endpoint for
  construído, não uma lacuna desta tarefa.
- **Sem verificação de que o conteúdo servido é de fato um PNG válido** — `FileSystemIconStorage`
  devolve qualquer byte que estiver no arquivo referenciado, com `Content-Type: image/png` fixo.
  Aceitável enquanto `icon_ref` só vem do seed controlado; mesma nota do risco anterior se isso
  mudar.

---

**Nota de processo — "segue com E-01" redirecionado para T-501.** Frederico pediu para seguir com
E-01, o épico de infraestrutura (34 pts, caminho crítico). Antes de tentar qualquer coisa, verifiquei
o conteúdo real de E-01: aquisição de hardware (T-101, especificação já pronta desde S005, critério
de aceite "nota fiscal emitida e equipamento recebido") e um roteiro de implantação física
(T-102–T-107: Hyper-V, promoção de controlador de domínio AD DS real, pilha RDS, FSLogix, AppLocker,
GPOs, ingresso de estações, varredura externa contra o IP público do escritório). Nenhuma dessas
tarefas é código — são ações físicas/operacionais que só Frederico (ou quem ele designar) pode
executar, fora deste ambiente de desenvolvimento Linux em sandbox, que não tem Windows Server,
hardware físico nem rede real para escanear. As duas especificações (`T-101-especificacao-de-
aquisicao.md`, `roteiro-implantacao.md`) já estavam completas e prontas desde S005 — não havia nada
de documentação para revisar sem informação nova (comprovante de compra, resultado de varredura
etc.) que só existiria depois da execução real.

Em vez de fingir progresso ou inventar trabalho, expliquei a situação e usei `AskUserQuestion` com
três opções: voltar para código (E-05), registrar progresso real de E-01 (se Frederico já tivesse
avançado fisicamente) ou revisar a especificação. A resposta foi "você decide". Escolhi voltar para
código — especificamente **T-501**, a primeira tarefa de E-05 que não depende de nenhuma peça ainda
não construída (`IRdpFileSigner`, T-502, e `ISessionBackend`, T-503, ambos ainda por fazer) — porque
é trabalho real, testável, que mantém o projeto avançando enquanto a aquisição de E-01 segue seu
próprio ritmo, fora do meu controle.

**O que foi feito (T-501):**
- Leitura de `ADR-0008` (tabela de política de redirecionamento), `API.md` §4 (`POST /v1/launches`,
  o contrato que vai consumir o `.rdp` gerado) e `ARQUITETURA.md` §4 (`RdpDescriptorBuilder`, RF-018)
  antes de escrever código.
- **Escopo confirmado antes de codificar**: `RdpDescriptorBuilder` monta só o texto do `.rdp` **não
  assinado**, a partir de parâmetros de conexão mínimos (host, alias, nome de exibição) — não
  resolve host (isso é `ISessionBackend`, T-503) nem assina (isso é `IRdpFileSigner`, T-502).
  Construir qualquer uma das duas coisas agora seria escopo além do que T-501 pede (RP-05).
- `src/AppBridge.ControlPlane.Infrastructure/Rdp/IRdpDescriptorBuilder.cs` +
  `RdpDescriptorBuilder.cs` (criados): cada linha de redirecionamento comentada com a linha exata de
  ADR-0008 que ela materializa — nenhuma propriedade de `.rdp` foi incluída sem essa referência
  (`redirectprinters`/`redirectsmartcards`/`redirectclipboard` permitidos;
  `drivestoredirect`/`devicestoredirect` vazios, negados; `redirectcomports` negado;
  `audiomode`/`audiocapturemode` — saída permitida, entrada negada).
- **Bug pego durante o próprio build, não em revisão nem em teste**: o namespace inicial
  `AppBridge.ControlPlane.Infrastructure.Launch` colidia com a entidade `Launch`
  (`Domain.Trail.Launch`, de T-202) — o compilador passou a resolver `DbSet<Launch>` em
  `AppBridgeDbContext.cs` e `LaunchConfiguration.cs` para o **namespace** novo em vez do **tipo**
  (`CS0118`), porque um namespace aninhado do mesmo nome dentro do namespace-pai (`Infrastructure`)
  sombreia um tipo externo de mesmo nome sem precisar de nenhum `using` nos arquivos afetados.
  Renomeado para `AppBridge.ControlPlane.Infrastructure.Rdp` antes de qualquer commit — build limpo
  confirmou, nenhum arquivo pré-existente precisou de ajuste além do rename do próprio diretório
  novo.
- Registrado em `Program.cs` como `Singleton` (sem estado, sem I/O) — mesmo padrão de
  `ISessionTokenIssuer` — sem consumidor real ainda (chega com T-504).
- **Verificado subindo a aplicação de verdade** (`dotnet run`): confirma que a injeção de dependência
  resolve sem erro; `GET /v1/health` continua respondendo `200`. Sem `curl` de um fluxo de
  lançamento — não há endpoint que use `IRdpDescriptorBuilder` ainda; isso é T-504.
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/RdpDescriptorBuilderTests.cs` (criado) — 10
  testes, um por propriedade (ou par relacionado): nega unidade local; permite impressora; permite
  smart card/token; permite área de transferência; nega porta COM; permite áudio de saída e nega
  áudio de entrada; nega Plug-and-Play genérico; monta modo RemoteApp com alias e nome corretos;
  aponta para o host correto; usa terminador de linha `\r\n` mesmo rodando neste ambiente Linux (o
  `.rdp` é lido por um cliente Windows, independente do sistema operacional que o gerou —
  `Environment.NewLine` teria sido o erro sutil aqui, e o código já usa `"\r\n"` literal, não
  `Environment.NewLine`).
- **91 testes automatizados no total** no Control Plane (39 Api + 52 Infrastructure), todos
  passando. **Nenhum bug de produção encontrado durante a verificação manual** — o único problema
  real desta tarefa (a colisão de namespace) apareceu no primeiro `dotnet build`, antes de qualquer
  teste ou verificação manual começar.

**Decisões tomadas (T-501):** nenhum ADR — execução direta de RF-018 e da tabela já decidida em
ADR-0008; uma decisão de processo registrada (o redirecionamento de E-01) e uma de implementação.

1. **Redirecionar de E-01 (não executável) para T-501 (E-05)**, em vez de tentar simular ou
   documentar em torno da impossibilidade de executar infraestrutura física — decisão tomada após
   `AskUserQuestion` e "você decide", registrada aqui para que a mudança de rumo não pareça ter
   ignorado o pedido original.
2. **`RdpConnectionParameters` como DTO mínimo**, sem tentar antecipar o formato que
   `ISessionBackend.BuildConnectionDescriptorAsync` (T-503) vai produzir — inventar essa integração
   agora seria acoplar T-501 a uma decisão que ainda não foi tomada.

**Arquivos criados/alterados (T-501):**
- `src/AppBridge.ControlPlane.Infrastructure/Rdp/IRdpDescriptorBuilder.cs` (criado)
- `src/AppBridge.ControlPlane.Infrastructure/Rdp/RdpDescriptorBuilder.cs` (criado)
- `src/AppBridge.ControlPlane.Api/Program.cs` (alterado — registro de `IRdpDescriptorBuilder`)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/RdpDescriptorBuilderTests.cs` (criado)
- `docs/ROADMAP.md` (alterado — T-501 marcada concluída, nota de conclusão e de redirecionamento)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-501):**
- **T-502** (`IRdpFileSigner` + `RdpSignExeSigner`) é a próxima de E-05 — **mesma classe de
  problema de E-01**: `rdpsign.exe` é uma ferramenta Windows real que não existe nesta sandbox Linux.
  A interface `IRdpFileSigner` pode ser desenhada e testada com um `fake`/`stub`, mas a implementação
  real (`RdpSignExeSigner`) não pode ser verificada de ponta a ponta aqui — vale decidir com
  Frederico, antes de começar, se isso é aceitável para T-502 ou se a tarefa espera E-01 fornecer um
  host Windows real para testar contra.
- **T-503** (`ISessionBackend` + `RdsSessionBackend`) tem a mesma limitação — `RdsSessionBackend`
  fala com um Connection Broker real que não existe aqui.
- **`IIdentityProvider` real** e **T-803** continuam as mesmas pendências já registradas.
- **E-01 em si** segue inteiramente pendente de ação de Frederico — aquisição de hardware é o
  gargalo declarado desde T-101 (R-023).
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-501):**
- **Nenhum risco novo de produto.** RF-018/ADR-0008 já prescreviam exatamente este comportamento.
- **T-502 e T-503 podem não ser inteiramente verificáveis nesta sandbox** — risco de processo, não de
  produto: qualquer implementação real que dependa de `rdpsign.exe`, RDS Connection Broker ou AD DS
  só pode ser testada de verdade quando E-01 (ou um ambiente Windows equivalente) existir. Vale
  decidir explicitamente com Frederico como tratar isso antes de aprovar T-502.

---

**O que foi feito (T-502, aprovada com "interface + fake testável, seguimos com T-502"):**
- Leitura de `ADR-0009` (a decisão já tomada: `IRdpFileSigner` com responsabilidade única, receber
  `.rdp` e devolver assinado; `RdpSignExeSigner` invoca `rdpsign.exe` num processo separado, com
  timeout, trata falha como erro de lançamento) e `API.md` §4 (`503 SIGNING_UNAVAILABLE`, "nenhum
  `.rdp` sai sem assinatura") antes de escrever código.
- **Escopo confirmado antes de codificar, seguindo a aprovação explícita**: construir a interface e
  a implementação real (`RdpSignExeSigner`, que de fato invoca `rdpsign.exe`), mas testar contra um
  **fake controlado**, não contra o binário verdadeiro — que não existe nesta sandbox Linux. O
  mapeamento HTTP "falha de assinatura → `503`" (parte do critério de aceite de T-502 no `ROADMAP.md`)
  fica para T-504 (`POST /v1/launches`), que ainda não existe — não há onde esse mapeamento morar
  hoje, e inventar um endpoint só para isso seria escopo além do que T-502 pede.
- `src/AppBridge.ControlPlane.Infrastructure/Rdp/IRdpFileSigner.cs` (criado): interface de um único
  método, `SignAsync(string unsignedRdpContent, CancellationToken)`, e
  `RdpSigningFailedException` — mesma disciplina de "falha vira exceção nomeada, não resultado
  degradado" de `AuditWriteFailedException` (T-205).
- `src/AppBridge.ControlPlane.Infrastructure/Rdp/RdpSignExeSigner.cs` (criado): grava o conteúdo
  num arquivo temporário (`rdpsign.exe` opera em arquivo, não em stdin/stdout — não é uma
  especificação pública, é exatamente esse binário, ADR-0009), invoca
  `rdpsign.exe /sha256 <thumbprint> <arquivo>` com `Process`, `stderr`/`stdout` drenados em paralelo
  ao `WaitForExitAsync` (evita deadlock de pipe cheio), timeout via `CancellationTokenSource` ligado
  ao token do chamador (distingue explicitamente "meu timeout interno expirou" de "o chamador
  cancelou" — só o primeiro vira `RdpSigningFailedException`, o segundo propaga
  `OperationCanceledException` normalmente), mata o processo se ainda estiver rodando ao sair por
  qualquer caminho, e **sempre apaga o arquivo temporário**, sucesso ou falha.
- `RdpSignerOptions`: `CertificateThumbprint` (obrigatório, de `APPBRIDGE_RDP_SIGNING_THUMBPRINT` —
  não é segredo, MODELO-DE-DADOS.md §3.4 já registra isso: a chave privada é o material sensível e
  nunca sai do repositório de certificados da máquina), `ExecutablePath` (padrão `"rdpsign.exe"`,
  resolvido por `PATH`, sobrescrevível por `APPBRIDGE_RDPSIGN_PATH`), `Timeout` (`PREMISSA:` 10 s,
  não medido — mesma categoria de PRE-12).
- **Scripts `fake-rdpsign-*.sh`** (`tests/AppBridge.ControlPlane.Infrastructure.Tests/fixtures/`,
  executáveis): `fake-rdpsign-success.sh` imita o contrato de linha de comando real
  (`/sha256 <thumbprint> <arquivo>`) gravando uma linha com o thumbprint recebido no arquivo —
  suficiente para provar que `RdpSignExeSigner` passa os argumentos certos e lê o arquivo de volta
  corretamente, sem precisar do binário verdadeiro; `fake-rdpsign-fail.sh` sai com código 1 e
  mensagem em `stderr`; `fake-rdpsign-hang.sh` dorme 30 s, para provar que o timeout mata o processo
  em vez de travar o chamador. Caminho do script resolvido via `[CallerFilePath]` a partir do
  arquivo de teste, não por cópia para o diretório de saída do build — mais simples que configurar
  `.csproj` para copiar arquivos não-`.cs`.
- **Verificado rodando os testes de verdade** (não simulado): a assinatura bem-sucedida grava e lê
  de volta o arquivo real; a falha devolve a mensagem de `stderr` real; o timeout mata o processo
  real (confirmado pelo tempo total de execução dos 4 testes — ~500ms, não os 30s que o `sleep` do
  fake pediria se o timeout não estivesse funcionando); o executável inexistente falha ao iniciar.
  Contagem de arquivos temporários (`appbridge-rdp-*.rdp`) verificada antes/depois de cada cenário —
  sucesso e falha — confirmando que nada fica para trás.
- **Program.cs**: `APPBRIDGE_RDP_SIGNING_THUMBPRINT` (obrigatória, mesmo padrão de
  `APPBRIDGE_DB_CONNECTION`/`APPBRIDGE_JWT_SIGNING_KEY`/`APPBRIDGE_ICON_STORAGE_PATH`) e
  `APPBRIDGE_RDPSIGN_PATH` (opcional). `IRdpFileSigner` registrado como `Singleton`.
- `ApiTestFactory.cs` (alterado): quarta variável de ambiente obrigatória, valor fixo de teste — nenhum
  teste de Api ainda chama `IRdpFileSigner` (isso é T-504), só precisa satisfazer a checagem de
  inicialização de `Program.cs`.
- **Verificado subindo a aplicação de verdade** (`dotnet run` com a variável nova definida):
  confirma que a injeção de dependência resolve sem erro; `GET /v1/health` continua respondendo
  `200`.
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/RdpSignExeSignerTests.cs` (criado) — 4 testes,
  descritos acima.
- **95 testes automatizados no total** no Control Plane (39 Api + 56 Infrastructure), todos
  passando. **Nenhum bug encontrado durante a verificação desta tarefa.**

**Decisões tomadas (T-502):** nenhum ADR — execução direta de ADR-0009, já aceito; duas decisões de
implementação registradas para não invenção de escopo, mais a decisão de processo já aprovada
explicitamente por Frederico.

1. **Testar contra fake, não contra o binário real** — aprovada explicitamente, não assumida. Deixa
   claro, em código e documentação, o limite exato do que foi verificado: orquestração de processo,
   não validade de assinatura RDP.
2. **Mapeamento HTTP de falha de assinatura para `503` não construído** — não há endpoint ainda
   (T-504); a exceção nomeada é o que existe hoje para um futuro chamador tratar.

**Arquivos criados/alterados (T-502):**
- `src/AppBridge.ControlPlane.Infrastructure/Rdp/IRdpFileSigner.cs` (criado)
- `src/AppBridge.ControlPlane.Infrastructure/Rdp/RdpSignExeSigner.cs` (criado)
- `src/AppBridge.ControlPlane.Api/Program.cs` (alterado — registro de `IRdpFileSigner`)
- `tests/AppBridge.ControlPlane.Api.Tests/ApiTestFactory.cs` (alterado — quarta variável)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/RdpSignExeSignerTests.cs` (criado)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/fixtures/fake-rdpsign-success.sh` (criado)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/fixtures/fake-rdpsign-fail.sh` (criado)
- `tests/AppBridge.ControlPlane.Infrastructure.Tests/fixtures/fake-rdpsign-hang.sh` (criado)
- `docs/SETUP-DEV.md` (alterado — §3 e nota em §7)
- `docs/ROADMAP.md` (alterado — T-502 marcada concluída, nota de conclusão)
- `docs/STATUS.md` (alterado — §1, §3)
- `docs/CHANGELOG.md` (alterado)
- `docs/auditoria/2026-08-10-S010.md` (este arquivo)

**Pendências (após T-502):**
- **T-503** (`ISessionBackend` + `RdsSessionBackend`) é a próxima de E-05 — mesma classe de
  limitação: fala com um Connection Broker real que não existe aqui. Mesma pergunta de processo que
  T-502 já respondeu provavelmente se aplica de novo.
- **T-504** (`POST /v1/launches`) é onde `IRdpDescriptorBuilder` e `IRdpFileSigner` finalmente têm
  consumidor real, e onde `RdpSigningFailedException` vira `503 SIGNING_UNAVAILABLE`.
- **`IIdentityProvider` real**, **T-803** e **E-01 em si** continuam as mesmas pendências já
  registradas.
- B-006, B-007 e B-009 continuam abertas.

**Riscos identificados (T-502):**
- **Nenhum risco novo de produto.** RF-018/RF-019/ADR-0009 já prescreviam exatamente este
  comportamento.
- **A assinatura real nunca foi exercitada.** `RdpSignExeSigner` está escrito e testado na sua
  orquestração de processo, mas nenhum teste aqui prova que `rdpsign.exe` de verdade aceita os
  argumentos na ordem que este código passa, nem que o `.rdp` resultante é aceito por uma estação
  Windows real. Primeira verificação de ponta a ponta só é possível quando E-01 existir — registrar
  isso como item explícito do roteiro de verificação antes do dogfood (T-005), não assumir que "os
  testes passam" equivale a "a assinatura funciona".

**Checagem de consistência desta sessão (RA-02, item 4):** a executar ao final, antes do commit.
